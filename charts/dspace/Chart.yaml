apiVersion: v2
name: dspace
# Keep the chart version in sync with docs/apps/dspace.version (consumed by sugarkube tooling).
version: 3.0.0
description: Helm chart for deploying the DSPACE application
type: application
appVersion: "v3.0.0"
