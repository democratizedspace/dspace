apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: {{ include "dspace.fullname" . }}-pdb
  labels:
    {{- include "dspace.labels" . | nindent 4 }}
spec:
  minAvailable: 1
  selector:
    matchLabels:
      {{- include "dspace.selectorLabels" . | nindent 6 }}
