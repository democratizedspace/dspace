1. Get the application URL by running these commands:
{{- if .Values.ingress.enabled }}
  echo "https://{{ (index .Values.ingress.hosts 0).host }}"
{{- else if contains "NodePort" .Values.service.type }}
  export NODE_PORT=$(kubectl get --namespace {{ .Release.Namespace }} -o jsonpath="{.spec.ports[0].nodePort}" services {{ include "dspace.fullname" . }})
  export NODE_IP=$(kubectl get nodes --namespace {{ .Release.Namespace }} -o jsonpath="{.items[0].status.addresses[0].address}")
  echo "http://$NODE_IP:$NODE_PORT"
{{- else }}
  kubectl get --namespace {{ .Release.Namespace }} svc {{ include "dspace.fullname" . }}
{{- end }}
