{
  "id": "offline-toast-src-path",
  "date": "2025-12-12",
  "component": "frontend:build",
  "rootCause": "A hard-coded /src/scripts/offlineToast.js import in the layout leaked into the 404 server chunk, leaving unbundled source paths in build artifacts on Windows and Linux.",
  "resolution": "Relocated offlineToast.js into frontend/src/scripts and resolved it with Astro.resolve inside Layout.astro so the bundled asset path is emitted instead of any /src reference.",
  "references": [
    "frontend/src/scripts/offlineToast.js",
    "frontend/src/layouts/Layout.astro",
    "tests/buildOutputValidation.test.ts",
    "tests/offlineToast.test.ts"
  ]
}
