{
  "id": "offline-toast-src-path",
  "date": "2025-12-12",
  "component": "frontend:build",
  "rootCause": "The Layout inline script imported offlineToast from /src/scripts, leaving an unbundled /src/scripts/offlineToast.js reference in the 404 build output and breaking Windows/Linux parity checks.",
  "resolution": "Moved offlineToast.js to frontend/public/scripts and switched Layout.astro to import it from the public /scripts path so built pages no longer leak source-only URLs.",
  "references": [
    "frontend/public/scripts/offlineToast.js",
    "frontend/src/layouts/Layout.astro",
    "tests/buildOutputValidation.test.ts"
  ]
}
