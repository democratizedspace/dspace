{
  "id": "2026-01-21-process-form-create-assertion",
  "date": "2026-01-21",
  "component": "frontend/tests",
  "rootCause": "The ProcessForm unit test asserted that createProcess received a single object payload, but the implementation calls createProcess with positional arguments, so the mock assertion never matched and the test timed out.",
  "resolution": "Update the unit test to assert the expected positional arguments, matching the current createProcess signature.",
  "references": [
    "frontend/src/components/__tests__/ProcessForm.spec.ts"
  ]
}
