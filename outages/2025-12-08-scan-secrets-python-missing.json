{
  "id": "2025-12-08-scan-secrets-python-missing",
  "date": "2025-12-08",
  "component": "pre-push hooks",
  "rootCause": "Root tests assumed python3 was available locally, causing scan-secrets script invocations to fail on Windows where Python was absent.",
  "resolution": "Added a python3 presence guard to conditionally run the scan-secrets tests, keeping CI coverage while skipping on hosts without Python.",
  "references": [
    "tests/scanSecretsScript.test.ts",
    "scripts/scan-secrets.py"
  ]
}
