# v3 quest quality review: manual QA signal vs newly added quest content

## Why this doc exists

The v3 QA checklist already has a quest-by-quest validation list in
`docs/qa/v3.md` §4.5. At the time of writing, only 16 quests are checked, while 236 remain unchecked.
Most unchecked quests are newly added in v3 (`docs/new-quests.md`) and deserve focused quality review
before release hardening.

This doc compares the checked quests against unchecked quests, identifies recurring anti-patterns
in lower-quality entries, and proposes concrete documentation improvements for both humans and LLM
agents authoring quests.

## Scope and method

- Source of checked/unchecked status: `docs/qa/v3.md` §4.5.
- Source of “new in v3” status: `docs/new-quests.md`.
- Source of content quality observations: quest JSON under
  `frontend/src/pages/quests/json/<tree>/<quest>.json`.

Interpretation rule for this document:

- “Checked quests” always means entries marked checked in `docs/qa/v3.md` §4.5.
- “New in v3” always means quests listed in `docs/new-quests.md`.

Quick quantitative scan (scripted locally):

- Scan run on **2026-02-16** at commit **`80eb7ce9dc11c30817089a511277fc29832026f2`**.
- Reproduction method: parse quest IDs from `docs/qa/v3.md` §4.5, intersect with `docs/new-quests.md`, then compute structural counts from corresponding quest JSON files.

- Checked quests: **16**
- Unchecked quests: **236**
- Checked quests that are new in v3: **15/16**
- Unchecked quests that are new in v3: **213/236**

Average structural complexity (dialogue graph only):

- Checked quests: **8.81 dialogue nodes**, **15.81 options**, **2.12 process options**,
  **3.19 gated options**, **2.19 quiz-like nodes**.
- Unchecked quests: **3.81 dialogue nodes**, **5.31 options**, **1.17 process options**,
  **1.95 gated options**, **~0 quiz-like nodes**.

## Manually validated quest set (from `docs/qa/v3.md` §4.5)

Checked quest IDs (16 total):

- `3dprinting/start`
- `composting/check-temperature`
- `composting/sift-compost`
- `composting/start`
- `composting/turn-pile`
- `hydroponics/basil`
- `hydroponics/nutrient-check`
- `sysadmin/basic-commands`
- `sysadmin/log-analysis`
- `sysadmin/resource-monitoring`
- `ubi/basicincome`
- `welcome/connect-github`
- `welcome/howtodoquests`
- `welcome/intro-inventory`
- `welcome/run-tests`
- `welcome/smart-plug-test`

Exemplars from the checked set (what they do well):

- `hydroponics/nutrient-check`: ties progress to concrete process + item gating, not just narration.
- `sysadmin/basic-commands`: includes interpretation checks that build operator literacy and recap.
- `composting/start`: combines setup/safety/science checkpoints with retry loops.
- `welcome/run-tests`: bridges player action to next-step workflow expectations.

Contrast with unchecked quests that are also listed as new in v3 (`docs/new-quests.md`):

- `astronomy/saturn-rings`: currently tends toward a thin shell (observe then finish) with little
  troubleshooting path.
- `devops/fail2ban`: lacks explicit staged verification artifacts for install/config/validate flow.
- `energy/dWatt-1e8`: accumulation-heavy target with limited strategic branching.
- `programming/web-server`: can finish without strong failure-mode handling or evidence artifacts.

## What the checked quests are doing better

Checked quests are not uniformly perfect, but they more often demonstrate:

1. **Interaction depth**
   - Multi-step progression with explicit setup, execution, and validation flow.
   - Example: `composting/start` walks through UX, materials, safety/science, and quiz retries.
2. **Mechanic grounding**
   - Uses DSPACE mechanics directly (processes on item pages, gated transitions, item proofs).
   - Example: `hydroponics/nutrient-check` uses multiple process steps plus item gates.
3. **Operator literacy / explainability**
   - Some quests teach reasoning rather than only requiring a single item threshold.
   - Example: `sysadmin/basic-commands` includes command interpretation and knowledge checks.
4. **Safety and reproducibility cues**
   - Better entries include concrete safety behavior and realistic sequencing.

## Common anti-patterns in unchecked quests (especially new in v3)

1. **Three-node “thin shell” quests**
   - Pattern: `start -> instruction -> finish` with one gate and no branching.
   - Risk: feels transactional, low retention, easy to auto-generate but not meaningful.

2. **Checklist prose without in-game verification**
   - Long instruction text appears in one node, but there is little/no mechanic-level evidence that
     the player performed intermediate steps.

3. **Progress by accumulation only (numeric grind ladders)**
   - Especially visible in `energy/dWatt-*` style quests.
   - Risk: inflated goals with minimal narrative/mechanical variety.

4. **Generic rewards and weak differentiation**
   - Many quests grant the same trophy-like item regardless of topic/effort.
   - Risk: progression and domain identity blur together.

5. **Hardening metadata optimism without corresponding depth**
   - Some quests have high scores or pass markers while still structurally thin.
   - Risk: hardening fields become decorative instead of quality evidence.

6. **Terminology and identity drift**
   - Example: file path `electronics/soldering-intro.json` has ID `electronics/tin-soldering-iron`.
   - Risk: harder diagnostics, author confusion, and weaker canonical mapping.

## Problematic quests to prioritize (with improvement checklist)

Use this as an actionable backlog for quest polishing passes. This section now enumerates **every unchecked quest that was newly added in v3**.

- Newly added in v3 (`docs/new-quests.md`): **225**
- Checked in `docs/qa/v3.md` §4.5 that are also new in v3: **12**
- Remaining newly added v3 quests with quality gaps to prioritize: **213**

### 3dprinting (12 quests)

- `3dprinting/bed-leveling` — **Issues:** calibration/maintenance flow is shallow; print-quality evidence gate missing; failure recovery for common print defects is underdeveloped. **Parity upgrades:** Add a branch that handles the most likely failure mode before `finish` unlocks. Gate completion on a mechanics-backed artifact (process output, measured item, or parsed log). Mirror pacing and explanatory depth from checked exemplars in `docs/qa/v3.md` §4.5 for this domain.
- `3dprinting/blob-of-death` — **Issues:** calibration/maintenance flow is shallow; print-quality evidence gate missing; failure recovery for common print defects is underdeveloped. **Parity upgrades:** Add a branch that handles the most likely failure mode before `finish` unlocks. Gate completion on a mechanics-backed artifact (process output, measured item, or parsed log). Mirror pacing and explanatory depth from checked exemplars in `docs/qa/v3.md` §4.5 for this domain.
- `3dprinting/cable-clip` — **Issues:** calibration/maintenance flow is shallow; print-quality evidence gate missing; failure recovery for common print defects is underdeveloped. **Parity upgrades:** Add a branch that handles the most likely failure mode before `finish` unlocks. Gate completion on a mechanics-backed artifact (process output, measured item, or parsed log). Mirror pacing and explanatory depth from checked exemplars in `docs/qa/v3.md` §4.5 for this domain.
- `3dprinting/calibration-cube` — **Issues:** calibration/maintenance flow is shallow; print-quality evidence gate missing; failure recovery for common print defects is underdeveloped. **Parity upgrades:** Add a branch that handles the most likely failure mode before `finish` unlocks. Gate completion on a mechanics-backed artifact (process output, measured item, or parsed log). Mirror pacing and explanatory depth from checked exemplars in `docs/qa/v3.md` §4.5 for this domain.
- `3dprinting/filament-change` — **Issues:** calibration/maintenance flow is shallow; print-quality evidence gate missing; failure recovery for common print defects is underdeveloped. **Parity upgrades:** Add a branch that handles the most likely failure mode before `finish` unlocks. Gate completion on a mechanics-backed artifact (process output, measured item, or parsed log). Mirror pacing and explanatory depth from checked exemplars in `docs/qa/v3.md` §4.5 for this domain.
- `3dprinting/nozzle-cleaning` — **Issues:** calibration/maintenance flow is shallow; print-quality evidence gate missing; failure recovery for common print defects is underdeveloped. **Parity upgrades:** Add a branch that handles the most likely failure mode before `finish` unlocks. Gate completion on a mechanics-backed artifact (process output, measured item, or parsed log). Mirror pacing and explanatory depth from checked exemplars in `docs/qa/v3.md` §4.5 for this domain.
- `3dprinting/nozzle-clog` — **Issues:** calibration/maintenance flow is shallow; print-quality evidence gate missing; failure recovery for common print defects is underdeveloped. **Parity upgrades:** Add a branch that handles the most likely failure mode before `finish` unlocks. Gate completion on a mechanics-backed artifact (process output, measured item, or parsed log). Mirror pacing and explanatory depth from checked exemplars in `docs/qa/v3.md` §4.5 for this domain.
- `3dprinting/phone-stand` — **Issues:** calibration/maintenance flow is shallow; print-quality evidence gate missing; failure recovery for common print defects is underdeveloped. **Parity upgrades:** Add a branch that handles the most likely failure mode before `finish` unlocks. Gate completion on a mechanics-backed artifact (process output, measured item, or parsed log). Mirror pacing and explanatory depth from checked exemplars in `docs/qa/v3.md` §4.5 for this domain.
- `3dprinting/retraction-test` — **Issues:** calibration/maintenance flow is shallow; print-quality evidence gate missing; failure recovery for common print defects is underdeveloped. **Parity upgrades:** Add a branch that handles the most likely failure mode before `finish` unlocks. Gate completion on a mechanics-backed artifact (process output, measured item, or parsed log). Mirror pacing and explanatory depth from checked exemplars in `docs/qa/v3.md` §4.5 for this domain.
- `3dprinting/spool-holder` — **Issues:** calibration/maintenance flow is shallow; print-quality evidence gate missing; failure recovery for common print defects is underdeveloped. **Parity upgrades:** Add a branch that handles the most likely failure mode before `finish` unlocks. Gate completion on a mechanics-backed artifact (process output, measured item, or parsed log). Mirror pacing and explanatory depth from checked exemplars in `docs/qa/v3.md` §4.5 for this domain.
- `3dprinting/temperature-tower` — **Issues:** calibration/maintenance flow is shallow; print-quality evidence gate missing; failure recovery for common print defects is underdeveloped. **Parity upgrades:** Add a branch that handles the most likely failure mode before `finish` unlocks. Gate completion on a mechanics-backed artifact (process output, measured item, or parsed log). Mirror pacing and explanatory depth from checked exemplars in `docs/qa/v3.md` §4.5 for this domain.
- `3dprinting/x-belt-tension` — **Issues:** calibration/maintenance flow is shallow; print-quality evidence gate missing; failure recovery for common print defects is underdeveloped. **Parity upgrades:** Add a branch that handles the most likely failure mode before `finish` unlocks. Gate completion on a mechanics-backed artifact (process output, measured item, or parsed log). Mirror pacing and explanatory depth from checked exemplars in `docs/qa/v3.md` §4.5 for this domain.

### aquaria (18 quests)

- `aquaria/aquarium-light` — **Issues:** husbandry steps collapse into single confirmation; water-parameter evidence is weak; no contingency branch for stress or unsafe readings. **Parity upgrades:** Add a branch that handles the most likely failure mode before `finish` unlocks. Gate completion on a mechanics-backed artifact (process output, measured item, or parsed log). Mirror pacing and explanatory depth from checked exemplars in `docs/qa/v3.md` §4.5 for this domain.
- `aquaria/balance-ph` — **Issues:** husbandry steps collapse into single confirmation; water-parameter evidence is weak; no contingency branch for stress or unsafe readings. **Parity upgrades:** Add a branch that handles the most likely failure mode before `finish` unlocks. Gate completion on a mechanics-backed artifact (process output, measured item, or parsed log). Mirror pacing and explanatory depth from checked exemplars in `docs/qa/v3.md` §4.5 for this domain.
- `aquaria/breeding` — **Issues:** husbandry steps collapse into single confirmation; water-parameter evidence is weak; no contingency branch for stress or unsafe readings. **Parity upgrades:** Add a branch that handles the most likely failure mode before `finish` unlocks. Gate completion on a mechanics-backed artifact (process output, measured item, or parsed log). Mirror pacing and explanatory depth from checked exemplars in `docs/qa/v3.md` §4.5 for this domain.
- `aquaria/filter-rinse` — **Issues:** husbandry steps collapse into single confirmation; water-parameter evidence is weak; no contingency branch for stress or unsafe readings. **Parity upgrades:** Add a branch that handles the most likely failure mode before `finish` unlocks. Gate completion on a mechanics-backed artifact (process output, measured item, or parsed log). Mirror pacing and explanatory depth from checked exemplars in `docs/qa/v3.md` §4.5 for this domain.
- `aquaria/floating-plants` — **Issues:** husbandry steps collapse into single confirmation; water-parameter evidence is weak; no contingency branch for stress or unsafe readings. **Parity upgrades:** Add a branch that handles the most likely failure mode before `finish` unlocks. Gate completion on a mechanics-backed artifact (process output, measured item, or parsed log). Mirror pacing and explanatory depth from checked exemplars in `docs/qa/v3.md` §4.5 for this domain.
- `aquaria/guppy` — **Issues:** husbandry steps collapse into single confirmation; water-parameter evidence is weak; no contingency branch for stress or unsafe readings. **Parity upgrades:** Add a branch that handles the most likely failure mode before `finish` unlocks. Gate completion on a mechanics-backed artifact (process output, measured item, or parsed log). Mirror pacing and explanatory depth from checked exemplars in `docs/qa/v3.md` §4.5 for this domain.
- `aquaria/heater-install` — **Issues:** installation flow lacks verification/rollback; water-parameter evidence is weak; no contingency branch for stress or unsafe readings. **Parity upgrades:** Split install→verify→rollback with distinct gates and outputs Gate completion on a mechanics-backed artifact (process output, measured item, or parsed log). Mirror pacing and explanatory depth from checked exemplars in `docs/qa/v3.md` §4.5 for this domain.
- `aquaria/log-water-parameters` — **Issues:** logging objective lacks acceptance criteria; water-parameter evidence is weak; no contingency branch for stress or unsafe readings. **Parity upgrades:** Add required log artifact + interpretation node that gates completion Gate completion on a mechanics-backed artifact (process output, measured item, or parsed log). Mirror pacing and explanatory depth from checked exemplars in `docs/qa/v3.md` §4.5 for this domain.
- `aquaria/net-fish` — **Issues:** husbandry steps collapse into single confirmation; water-parameter evidence is weak; no contingency branch for stress or unsafe readings. **Parity upgrades:** Add a branch that handles the most likely failure mode before `finish` unlocks. Gate completion on a mechanics-backed artifact (process output, measured item, or parsed log). Mirror pacing and explanatory depth from checked exemplars in `docs/qa/v3.md` §4.5 for this domain.
- `aquaria/ph-strip-test` — **Issues:** husbandry steps collapse into single confirmation; water-parameter evidence is weak; no contingency branch for stress or unsafe readings. **Parity upgrades:** Add a branch that handles the most likely failure mode before `finish` unlocks. Gate completion on a mechanics-backed artifact (process output, measured item, or parsed log). Mirror pacing and explanatory depth from checked exemplars in `docs/qa/v3.md` §4.5 for this domain.
- `aquaria/position-tank` — **Issues:** husbandry steps collapse into single confirmation; water-parameter evidence is weak; no contingency branch for stress or unsafe readings. **Parity upgrades:** Add a branch that handles the most likely failure mode before `finish` unlocks. Gate completion on a mechanics-backed artifact (process output, measured item, or parsed log). Mirror pacing and explanatory depth from checked exemplars in `docs/qa/v3.md` §4.5 for this domain.
- `aquaria/shrimp` — **Issues:** husbandry steps collapse into single confirmation; water-parameter evidence is weak; no contingency branch for stress or unsafe readings. **Parity upgrades:** Add a branch that handles the most likely failure mode before `finish` unlocks. Gate completion on a mechanics-backed artifact (process output, measured item, or parsed log). Mirror pacing and explanatory depth from checked exemplars in `docs/qa/v3.md` §4.5 for this domain.
- `aquaria/sponge-filter` — **Issues:** husbandry steps collapse into single confirmation; water-parameter evidence is weak; no contingency branch for stress or unsafe readings. **Parity upgrades:** Add a branch that handles the most likely failure mode before `finish` unlocks. Gate completion on a mechanics-backed artifact (process output, measured item, or parsed log). Mirror pacing and explanatory depth from checked exemplars in `docs/qa/v3.md` §4.5 for this domain.
- `aquaria/thermometer` — **Issues:** husbandry steps collapse into single confirmation; water-parameter evidence is weak; no contingency branch for stress or unsafe readings. **Parity upgrades:** Add a branch that handles the most likely failure mode before `finish` unlocks. Gate completion on a mechanics-backed artifact (process output, measured item, or parsed log). Mirror pacing and explanatory depth from checked exemplars in `docs/qa/v3.md` §4.5 for this domain.
- `aquaria/top-off` — **Issues:** husbandry steps collapse into single confirmation; water-parameter evidence is weak; no contingency branch for stress or unsafe readings. **Parity upgrades:** Add a branch that handles the most likely failure mode before `finish` unlocks. Gate completion on a mechanics-backed artifact (process output, measured item, or parsed log). Mirror pacing and explanatory depth from checked exemplars in `docs/qa/v3.md` §4.5 for this domain.
- `aquaria/walstad` — **Issues:** husbandry steps collapse into single confirmation; water-parameter evidence is weak; no contingency branch for stress or unsafe readings. **Parity upgrades:** Add a branch that handles the most likely failure mode before `finish` unlocks. Gate completion on a mechanics-backed artifact (process output, measured item, or parsed log). Mirror pacing and explanatory depth from checked exemplars in `docs/qa/v3.md` §4.5 for this domain.
- `aquaria/water-change` — **Issues:** husbandry steps collapse into single confirmation; water-parameter evidence is weak; no contingency branch for stress or unsafe readings. **Parity upgrades:** Add a branch that handles the most likely failure mode before `finish` unlocks. Gate completion on a mechanics-backed artifact (process output, measured item, or parsed log). Mirror pacing and explanatory depth from checked exemplars in `docs/qa/v3.md` §4.5 for this domain.
- `aquaria/water-testing` — **Issues:** husbandry steps collapse into single confirmation; water-parameter evidence is weak; no contingency branch for stress or unsafe readings. **Parity upgrades:** Add a branch that handles the most likely failure mode before `finish` unlocks. Gate completion on a mechanics-backed artifact (process output, measured item, or parsed log). Mirror pacing and explanatory depth from checked exemplars in `docs/qa/v3.md` §4.5 for this domain.

### astronomy (21 quests)

- `astronomy/andromeda` — **Issues:** observation chain often ends after a single sighting; artifact proof (log/sketch/photo) is optional or absent; weather/light-pollution fallback branch is missing. **Parity upgrades:** Add a branch that handles the most likely failure mode before `finish` unlocks. Gate completion on a mechanics-backed artifact (process output, measured item, or parsed log). Mirror pacing and explanatory depth from checked exemplars in `docs/qa/v3.md` §4.5 for this domain.
- `astronomy/aurora-watch` — **Issues:** observation chain often ends after a single sighting; artifact proof (log/sketch/photo) is optional or absent; weather/light-pollution fallback branch is missing. **Parity upgrades:** Add a branch that handles the most likely failure mode before `finish` unlocks. Gate completion on a mechanics-backed artifact (process output, measured item, or parsed log). Mirror pacing and explanatory depth from checked exemplars in `docs/qa/v3.md` §4.5 for this domain.
- `astronomy/basic-telescope` — **Issues:** observation chain often ends after a single sighting; artifact proof (log/sketch/photo) is optional or absent; weather/light-pollution fallback branch is missing. **Parity upgrades:** Add a branch that handles the most likely failure mode before `finish` unlocks. Gate completion on a mechanics-backed artifact (process output, measured item, or parsed log). Mirror pacing and explanatory depth from checked exemplars in `docs/qa/v3.md` §4.5 for this domain.
- `astronomy/binary-star` — **Issues:** observation chain often ends after a single sighting; artifact proof (log/sketch/photo) is optional or absent; weather/light-pollution fallback branch is missing. **Parity upgrades:** Add a branch that handles the most likely failure mode before `finish` unlocks. Gate completion on a mechanics-backed artifact (process output, measured item, or parsed log). Mirror pacing and explanatory depth from checked exemplars in `docs/qa/v3.md` §4.5 for this domain.
- `astronomy/comet-tracking` — **Issues:** observation chain often ends after a single sighting; artifact proof (log/sketch/photo) is optional or absent; weather/light-pollution fallback branch is missing. **Parity upgrades:** Add a branch that handles the most likely failure mode before `finish` unlocks. Gate completion on a mechanics-backed artifact (process output, measured item, or parsed log). Mirror pacing and explanatory depth from checked exemplars in `docs/qa/v3.md` §4.5 for this domain.
- `astronomy/constellations` — **Issues:** observation chain often ends after a single sighting; artifact proof (log/sketch/photo) is optional or absent; weather/light-pollution fallback branch is missing. **Parity upgrades:** Add a branch that handles the most likely failure mode before `finish` unlocks. Gate completion on a mechanics-backed artifact (process output, measured item, or parsed log). Mirror pacing and explanatory depth from checked exemplars in `docs/qa/v3.md` §4.5 for this domain.
- `astronomy/iss-flyover` — **Issues:** observation chain often ends after a single sighting; artifact proof (log/sketch/photo) is optional or absent; weather/light-pollution fallback branch is missing. **Parity upgrades:** Add a branch that handles the most likely failure mode before `finish` unlocks. Gate completion on a mechanics-backed artifact (process output, measured item, or parsed log). Mirror pacing and explanatory depth from checked exemplars in `docs/qa/v3.md` §4.5 for this domain.
- `astronomy/iss-photo` — **Issues:** observation chain often ends after a single sighting; artifact proof (log/sketch/photo) is optional or absent; weather/light-pollution fallback branch is missing. **Parity upgrades:** Add a branch that handles the most likely failure mode before `finish` unlocks. Gate completion on a mechanics-backed artifact (process output, measured item, or parsed log). Mirror pacing and explanatory depth from checked exemplars in `docs/qa/v3.md` §4.5 for this domain.
- `astronomy/jupiter-moons` — **Issues:** observation chain often ends after a single sighting; artifact proof (log/sketch/photo) is optional or absent; weather/light-pollution fallback branch is missing. **Parity upgrades:** Add a branch that handles the most likely failure mode before `finish` unlocks. Gate completion on a mechanics-backed artifact (process output, measured item, or parsed log). Mirror pacing and explanatory depth from checked exemplars in `docs/qa/v3.md` §4.5 for this domain.
- `astronomy/light-pollution` — **Issues:** observation chain often ends after a single sighting; artifact proof (log/sketch/photo) is optional or absent; weather/light-pollution fallback branch is missing. **Parity upgrades:** Add a branch that handles the most likely failure mode before `finish` unlocks. Gate completion on a mechanics-backed artifact (process output, measured item, or parsed log). Mirror pacing and explanatory depth from checked exemplars in `docs/qa/v3.md` §4.5 for this domain.
- `astronomy/lunar-eclipse` — **Issues:** observation chain often ends after a single sighting; artifact proof (log/sketch/photo) is optional or absent; weather/light-pollution fallback branch is missing. **Parity upgrades:** Add a branch that handles the most likely failure mode before `finish` unlocks. Gate completion on a mechanics-backed artifact (process output, measured item, or parsed log). Mirror pacing and explanatory depth from checked exemplars in `docs/qa/v3.md` §4.5 for this domain.
- `astronomy/meteor-shower` — **Issues:** observation chain often ends after a single sighting; artifact proof (log/sketch/photo) is optional or absent; weather/light-pollution fallback branch is missing. **Parity upgrades:** Add a branch that handles the most likely failure mode before `finish` unlocks. Gate completion on a mechanics-backed artifact (process output, measured item, or parsed log). Mirror pacing and explanatory depth from checked exemplars in `docs/qa/v3.md` §4.5 for this domain.
- `astronomy/north-star` — **Issues:** observation chain often ends after a single sighting; artifact proof (log/sketch/photo) is optional or absent; weather/light-pollution fallback branch is missing. **Parity upgrades:** Add a branch that handles the most likely failure mode before `finish` unlocks. Gate completion on a mechanics-backed artifact (process output, measured item, or parsed log). Mirror pacing and explanatory depth from checked exemplars in `docs/qa/v3.md` §4.5 for this domain.
- `astronomy/observe-moon` — **Issues:** observation chain often ends after a single sighting; artifact proof (log/sketch/photo) is optional or absent; weather/light-pollution fallback branch is missing. **Parity upgrades:** Add a branch that handles the most likely failure mode before `finish` unlocks. Gate completion on a mechanics-backed artifact (process output, measured item, or parsed log). Mirror pacing and explanatory depth from checked exemplars in `docs/qa/v3.md` §4.5 for this domain.
- `astronomy/orion-nebula` — **Issues:** observation chain often ends after a single sighting; artifact proof (log/sketch/photo) is optional or absent; weather/light-pollution fallback branch is missing. **Parity upgrades:** Add a branch that handles the most likely failure mode before `finish` unlocks. Gate completion on a mechanics-backed artifact (process output, measured item, or parsed log). Mirror pacing and explanatory depth from checked exemplars in `docs/qa/v3.md` §4.5 for this domain.
- `astronomy/planetary-alignment` — **Issues:** observation chain often ends after a single sighting; artifact proof (log/sketch/photo) is optional or absent; weather/light-pollution fallback branch is missing. **Parity upgrades:** Add a branch that handles the most likely failure mode before `finish` unlocks. Gate completion on a mechanics-backed artifact (process output, measured item, or parsed log). Mirror pacing and explanatory depth from checked exemplars in `docs/qa/v3.md` §4.5 for this domain.
- `astronomy/satellite-pass` — **Issues:** observation chain often ends after a single sighting; artifact proof (log/sketch/photo) is optional or absent; weather/light-pollution fallback branch is missing. **Parity upgrades:** Add a branch that handles the most likely failure mode before `finish` unlocks. Gate completion on a mechanics-backed artifact (process output, measured item, or parsed log). Mirror pacing and explanatory depth from checked exemplars in `docs/qa/v3.md` §4.5 for this domain.
- `astronomy/saturn-rings` — **Issues:** observation chain often ends after a single sighting; artifact proof (log/sketch/photo) is optional or absent; weather/light-pollution fallback branch is missing. **Parity upgrades:** Add a branch that handles the most likely failure mode before `finish` unlocks. Gate completion on a mechanics-backed artifact (process output, measured item, or parsed log). Mirror pacing and explanatory depth from checked exemplars in `docs/qa/v3.md` §4.5 for this domain.
- `astronomy/star-trails` — **Issues:** observation chain often ends after a single sighting; artifact proof (log/sketch/photo) is optional or absent; weather/light-pollution fallback branch is missing. **Parity upgrades:** Add a branch that handles the most likely failure mode before `finish` unlocks. Gate completion on a mechanics-backed artifact (process output, measured item, or parsed log). Mirror pacing and explanatory depth from checked exemplars in `docs/qa/v3.md` §4.5 for this domain.
- `astronomy/sunspot-sketch` — **Issues:** observation chain often ends after a single sighting; artifact proof (log/sketch/photo) is optional or absent; weather/light-pollution fallback branch is missing. **Parity upgrades:** Add a branch that handles the most likely failure mode before `finish` unlocks. Gate completion on a mechanics-backed artifact (process output, measured item, or parsed log). Mirror pacing and explanatory depth from checked exemplars in `docs/qa/v3.md` §4.5 for this domain.
- `astronomy/venus-phases` — **Issues:** observation chain often ends after a single sighting; artifact proof (log/sketch/photo) is optional or absent; weather/light-pollution fallback branch is missing. **Parity upgrades:** Add a branch that handles the most likely failure mode before `finish` unlocks. Gate completion on a mechanics-backed artifact (process output, measured item, or parsed log). Mirror pacing and explanatory depth from checked exemplars in `docs/qa/v3.md` §4.5 for this domain.

### chemistry (10 quests)

- `chemistry/acid-dilution` — **Issues:** lab workflow is compressed into short narration; safety checkpoints need stronger gating; measurement/result validation artifacts are thin. **Parity upgrades:** Add a branch that handles the most likely failure mode before `finish` unlocks. Gate completion on a mechanics-backed artifact (process output, measured item, or parsed log). Mirror pacing and explanatory depth from checked exemplars in `docs/qa/v3.md` §4.5 for this domain.
- `chemistry/acid-neutralization` — **Issues:** lab workflow is compressed into short narration; safety checkpoints need stronger gating; measurement/result validation artifacts are thin. **Parity upgrades:** Add a branch that handles the most likely failure mode before `finish` unlocks. Gate completion on a mechanics-backed artifact (process output, measured item, or parsed log). Mirror pacing and explanatory depth from checked exemplars in `docs/qa/v3.md` §4.5 for this domain.
- `chemistry/buffer-solution` — **Issues:** lab workflow is compressed into short narration; safety checkpoints need stronger gating; measurement/result validation artifacts are thin. **Parity upgrades:** Add a branch that handles the most likely failure mode before `finish` unlocks. Gate completion on a mechanics-backed artifact (process output, measured item, or parsed log). Mirror pacing and explanatory depth from checked exemplars in `docs/qa/v3.md` §4.5 for this domain.
- `chemistry/ph-adjustment` — **Issues:** lab workflow is compressed into short narration; safety checkpoints need stronger gating; measurement/result validation artifacts are thin. **Parity upgrades:** Add a branch that handles the most likely failure mode before `finish` unlocks. Gate completion on a mechanics-backed artifact (process output, measured item, or parsed log). Mirror pacing and explanatory depth from checked exemplars in `docs/qa/v3.md` §4.5 for this domain.
- `chemistry/ph-test` — **Issues:** lab workflow is compressed into short narration; safety checkpoints need stronger gating; measurement/result validation artifacts are thin. **Parity upgrades:** Add a branch that handles the most likely failure mode before `finish` unlocks. Gate completion on a mechanics-backed artifact (process output, measured item, or parsed log). Mirror pacing and explanatory depth from checked exemplars in `docs/qa/v3.md` §4.5 for this domain.
- `chemistry/precipitation-reaction` — **Issues:** lab workflow is compressed into short narration; safety checkpoints need stronger gating; measurement/result validation artifacts are thin. **Parity upgrades:** Add a branch that handles the most likely failure mode before `finish` unlocks. Gate completion on a mechanics-backed artifact (process output, measured item, or parsed log). Mirror pacing and explanatory depth from checked exemplars in `docs/qa/v3.md` §4.5 for this domain.
- `chemistry/safe-reaction` — **Issues:** lab workflow is compressed into short narration; safety checkpoints need stronger gating; measurement/result validation artifacts are thin. **Parity upgrades:** Add a branch that handles the most likely failure mode before `finish` unlocks. Gate completion on a mechanics-backed artifact (process output, measured item, or parsed log). Mirror pacing and explanatory depth from checked exemplars in `docs/qa/v3.md` §4.5 for this domain.
- `chemistry/stevia-crystals` — **Issues:** lab workflow is compressed into short narration; safety checkpoints need stronger gating; measurement/result validation artifacts are thin. **Parity upgrades:** Add a branch that handles the most likely failure mode before `finish` unlocks. Gate completion on a mechanics-backed artifact (process output, measured item, or parsed log). Mirror pacing and explanatory depth from checked exemplars in `docs/qa/v3.md` §4.5 for this domain.
- `chemistry/stevia-extraction` — **Issues:** lab workflow is compressed into short narration; safety checkpoints need stronger gating; measurement/result validation artifacts are thin. **Parity upgrades:** Add a branch that handles the most likely failure mode before `finish` unlocks. Gate completion on a mechanics-backed artifact (process output, measured item, or parsed log). Mirror pacing and explanatory depth from checked exemplars in `docs/qa/v3.md` §4.5 for this domain.
- `chemistry/stevia-tasting` — **Issues:** lab workflow is compressed into short narration; safety checkpoints need stronger gating; measurement/result validation artifacts are thin. **Parity upgrades:** Add a branch that handles the most likely failure mode before `finish` unlocks. Gate completion on a mechanics-backed artifact (process output, measured item, or parsed log). Mirror pacing and explanatory depth from checked exemplars in `docs/qa/v3.md` §4.5 for this domain.

### completionist (4 quests)

- `completionist/catalog` — **Issues:** meta-loop progression is mostly checklist prose; proof of breadth across systems is weak; reminder/escalation branches are minimal. **Parity upgrades:** Add a branch that handles the most likely failure mode before `finish` unlocks. Gate completion on a mechanics-backed artifact (process output, measured item, or parsed log). Mirror pacing and explanatory depth from checked exemplars in `docs/qa/v3.md` §4.5 for this domain.
- `completionist/display` — **Issues:** meta-loop progression is mostly checklist prose; proof of breadth across systems is weak; reminder/escalation branches are minimal. **Parity upgrades:** Add a branch that handles the most likely failure mode before `finish` unlocks. Gate completion on a mechanics-backed artifact (process output, measured item, or parsed log). Mirror pacing and explanatory depth from checked exemplars in `docs/qa/v3.md` §4.5 for this domain.
- `completionist/polish` — **Issues:** meta-loop progression is mostly checklist prose; proof of breadth across systems is weak; reminder/escalation branches are minimal. **Parity upgrades:** Add a branch that handles the most likely failure mode before `finish` unlocks. Gate completion on a mechanics-backed artifact (process output, measured item, or parsed log). Mirror pacing and explanatory depth from checked exemplars in `docs/qa/v3.md` §4.5 for this domain.
- `completionist/reminder` — **Issues:** meta-loop progression is mostly checklist prose; proof of breadth across systems is weak; reminder/escalation branches are minimal. **Parity upgrades:** Add a branch that handles the most likely failure mode before `finish` unlocks. Gate completion on a mechanics-backed artifact (process output, measured item, or parsed log). Mirror pacing and explanatory depth from checked exemplars in `docs/qa/v3.md` §4.5 for this domain.

### devops (15 quests)

- `devops/auto-updates` — **Issues:** runbook sequencing is under-specified; verification artifacts (logs/status checks) are inconsistent; rollback/safe-failure handling is often absent. **Parity upgrades:** Add a branch that handles the most likely failure mode before `finish` unlocks. Gate completion on a mechanics-backed artifact (process output, measured item, or parsed log). Mirror pacing and explanatory depth from checked exemplars in `docs/qa/v3.md` §4.5 for this domain.
- `devops/ci-pipeline` — **Issues:** runbook sequencing is under-specified; verification artifacts (logs/status checks) are inconsistent; rollback/safe-failure handling is often absent. **Parity upgrades:** Add a branch that handles the most likely failure mode before `finish` unlocks. Gate completion on a mechanics-backed artifact (process output, measured item, or parsed log). Mirror pacing and explanatory depth from checked exemplars in `docs/qa/v3.md` §4.5 for this domain.
- `devops/daily-backups` — **Issues:** runbook sequencing is under-specified; verification artifacts (logs/status checks) are inconsistent; rollback/safe-failure handling is often absent. **Parity upgrades:** Add a branch that handles the most likely failure mode before `finish` unlocks. Gate completion on a mechanics-backed artifact (process output, measured item, or parsed log). Mirror pacing and explanatory depth from checked exemplars in `docs/qa/v3.md` §4.5 for this domain.
- `devops/docker-compose` — **Issues:** runbook sequencing is under-specified; verification artifacts (logs/status checks) are inconsistent; rollback/safe-failure handling is often absent. **Parity upgrades:** Add a branch that handles the most likely failure mode before `finish` unlocks. Gate completion on a mechanics-backed artifact (process output, measured item, or parsed log). Mirror pacing and explanatory depth from checked exemplars in `docs/qa/v3.md` §4.5 for this domain.
- `devops/enable-https` — **Issues:** runbook sequencing is under-specified; verification artifacts (logs/status checks) are inconsistent; rollback/safe-failure handling is often absent. **Parity upgrades:** Add a branch that handles the most likely failure mode before `finish` unlocks. Gate completion on a mechanics-backed artifact (process output, measured item, or parsed log). Mirror pacing and explanatory depth from checked exemplars in `docs/qa/v3.md` §4.5 for this domain.
- `devops/fail2ban` — **Issues:** runbook sequencing is under-specified; verification artifacts (logs/status checks) are inconsistent; rollback/safe-failure handling is often absent. **Parity upgrades:** Add a branch that handles the most likely failure mode before `finish` unlocks. Gate completion on a mechanics-backed artifact (process output, measured item, or parsed log). Mirror pacing and explanatory depth from checked exemplars in `docs/qa/v3.md` §4.5 for this domain.
- `devops/firewall-rules` — **Issues:** runbook sequencing is under-specified; verification artifacts (logs/status checks) are inconsistent; rollback/safe-failure handling is often absent. **Parity upgrades:** Add a branch that handles the most likely failure mode before `finish` unlocks. Gate completion on a mechanics-backed artifact (process output, measured item, or parsed log). Mirror pacing and explanatory depth from checked exemplars in `docs/qa/v3.md` §4.5 for this domain.
- `devops/k3s-deploy` — **Issues:** runbook sequencing is under-specified; verification artifacts (logs/status checks) are inconsistent; rollback/safe-failure handling is often absent. **Parity upgrades:** Add a branch that handles the most likely failure mode before `finish` unlocks. Gate completion on a mechanics-backed artifact (process output, measured item, or parsed log). Mirror pacing and explanatory depth from checked exemplars in `docs/qa/v3.md` §4.5 for this domain.
- `devops/log-maintenance` — **Issues:** logging objective lacks acceptance criteria; verification artifacts (logs/status checks) are inconsistent; rollback/safe-failure handling is often absent. **Parity upgrades:** Add required log artifact + interpretation node that gates completion Gate completion on a mechanics-backed artifact (process output, measured item, or parsed log). Mirror pacing and explanatory depth from checked exemplars in `docs/qa/v3.md` §4.5 for this domain.
- `devops/monitoring` — **Issues:** monitoring objective lacks anomaly response; verification artifacts (logs/status checks) are inconsistent; rollback/safe-failure handling is often absent. **Parity upgrades:** Add anomaly branch with rollback/escalation and proof artifact Gate completion on a mechanics-backed artifact (process output, measured item, or parsed log). Mirror pacing and explanatory depth from checked exemplars in `docs/qa/v3.md` §4.5 for this domain.
- `devops/pi-cluster-hardware` — **Issues:** runbook sequencing is under-specified; verification artifacts (logs/status checks) are inconsistent; rollback/safe-failure handling is often absent. **Parity upgrades:** Add a branch that handles the most likely failure mode before `finish` unlocks. Gate completion on a mechanics-backed artifact (process output, measured item, or parsed log). Mirror pacing and explanatory depth from checked exemplars in `docs/qa/v3.md` §4.5 for this domain.
- `devops/prepare-first-node` — **Issues:** runbook sequencing is under-specified; verification artifacts (logs/status checks) are inconsistent; rollback/safe-failure handling is often absent. **Parity upgrades:** Add a branch that handles the most likely failure mode before `finish` unlocks. Gate completion on a mechanics-backed artifact (process output, measured item, or parsed log). Mirror pacing and explanatory depth from checked exemplars in `docs/qa/v3.md` §4.5 for this domain.
- `devops/private-registry` — **Issues:** runbook sequencing is under-specified; verification artifacts (logs/status checks) are inconsistent; rollback/safe-failure handling is often absent. **Parity upgrades:** Add a branch that handles the most likely failure mode before `finish` unlocks. Gate completion on a mechanics-backed artifact (process output, measured item, or parsed log). Mirror pacing and explanatory depth from checked exemplars in `docs/qa/v3.md` §4.5 for this domain.
- `devops/ssd-boot` — **Issues:** runbook sequencing is under-specified; verification artifacts (logs/status checks) are inconsistent; rollback/safe-failure handling is often absent. **Parity upgrades:** Add a branch that handles the most likely failure mode before `finish` unlocks. Gate completion on a mechanics-backed artifact (process output, measured item, or parsed log). Mirror pacing and explanatory depth from checked exemplars in `docs/qa/v3.md` §4.5 for this domain.
- `devops/ssh-hardening` — **Issues:** runbook sequencing is under-specified; verification artifacts (logs/status checks) are inconsistent; rollback/safe-failure handling is often absent. **Parity upgrades:** Add a branch that handles the most likely failure mode before `finish` unlocks. Gate completion on a mechanics-backed artifact (process output, measured item, or parsed log). Mirror pacing and explanatory depth from checked exemplars in `docs/qa/v3.md` §4.5 for this domain.

### electronics (22 quests)

- `electronics/arduino-blink` — **Issues:** build/test cycle is too linear; instrument-based verification is shallow; fault-isolation branch is missing for bad readings. **Parity upgrades:** Add a branch that handles the most likely failure mode before `finish` unlocks. Gate completion on a mechanics-backed artifact (process output, measured item, or parsed log). Mirror pacing and explanatory depth from checked exemplars in `docs/qa/v3.md` §4.5 for this domain.
- `electronics/basic-circuit` — **Issues:** build/test cycle is too linear; instrument-based verification is shallow; fault-isolation branch is missing for bad readings. **Parity upgrades:** Add a branch that handles the most likely failure mode before `finish` unlocks. Gate completion on a mechanics-backed artifact (process output, measured item, or parsed log). Mirror pacing and explanatory depth from checked exemplars in `docs/qa/v3.md` §4.5 for this domain.
- `electronics/check-battery-voltage` — **Issues:** verification step is single-shot without interpretation; instrument-based verification is shallow; fault-isolation branch is missing for bad readings. **Parity upgrades:** Add acceptable-range evaluation plus remediation branch before finish Gate completion on a mechanics-backed artifact (process output, measured item, or parsed log). Mirror pacing and explanatory depth from checked exemplars in `docs/qa/v3.md` §4.5 for this domain.
- `electronics/continuity-test` — **Issues:** build/test cycle is too linear; instrument-based verification is shallow; fault-isolation branch is missing for bad readings. **Parity upgrades:** Add a branch that handles the most likely failure mode before `finish` unlocks. Gate completion on a mechanics-backed artifact (process output, measured item, or parsed log). Mirror pacing and explanatory depth from checked exemplars in `docs/qa/v3.md` §4.5 for this domain.
- `electronics/data-logger` — **Issues:** build/test cycle is too linear; instrument-based verification is shallow; fault-isolation branch is missing for bad readings. **Parity upgrades:** Add a branch that handles the most likely failure mode before `finish` unlocks. Gate completion on a mechanics-backed artifact (process output, measured item, or parsed log). Mirror pacing and explanatory depth from checked exemplars in `docs/qa/v3.md` §4.5 for this domain.
- `electronics/desolder-component` — **Issues:** build/test cycle is too linear; instrument-based verification is shallow; fault-isolation branch is missing for bad readings. **Parity upgrades:** Add a branch that handles the most likely failure mode before `finish` unlocks. Gate completion on a mechanics-backed artifact (process output, measured item, or parsed log). Mirror pacing and explanatory depth from checked exemplars in `docs/qa/v3.md` §4.5 for this domain.
- `electronics/led-polarity` — **Issues:** build/test cycle is too linear; instrument-based verification is shallow; fault-isolation branch is missing for bad readings. **Parity upgrades:** Add a branch that handles the most likely failure mode before `finish` unlocks. Gate completion on a mechanics-backed artifact (process output, measured item, or parsed log). Mirror pacing and explanatory depth from checked exemplars in `docs/qa/v3.md` §4.5 for this domain.
- `electronics/light-sensor` — **Issues:** build/test cycle is too linear; instrument-based verification is shallow; fault-isolation branch is missing for bad readings. **Parity upgrades:** Add a branch that handles the most likely failure mode before `finish` unlocks. Gate completion on a mechanics-backed artifact (process output, measured item, or parsed log). Mirror pacing and explanatory depth from checked exemplars in `docs/qa/v3.md` §4.5 for this domain.
- `electronics/measure-arduino-5v` — **Issues:** build/test cycle is too linear; instrument-based verification is shallow; fault-isolation branch is missing for bad readings. **Parity upgrades:** Add a branch that handles the most likely failure mode before `finish` unlocks. Gate completion on a mechanics-backed artifact (process output, measured item, or parsed log). Mirror pacing and explanatory depth from checked exemplars in `docs/qa/v3.md` §4.5 for this domain.
- `electronics/measure-led-current` — **Issues:** build/test cycle is too linear; instrument-based verification is shallow; fault-isolation branch is missing for bad readings. **Parity upgrades:** Add a branch that handles the most likely failure mode before `finish` unlocks. Gate completion on a mechanics-backed artifact (process output, measured item, or parsed log). Mirror pacing and explanatory depth from checked exemplars in `docs/qa/v3.md` §4.5 for this domain.
- `electronics/measure-resistance` — **Issues:** build/test cycle is too linear; instrument-based verification is shallow; fault-isolation branch is missing for bad readings. **Parity upgrades:** Add a branch that handles the most likely failure mode before `finish` unlocks. Gate completion on a mechanics-backed artifact (process output, measured item, or parsed log). Mirror pacing and explanatory depth from checked exemplars in `docs/qa/v3.md` §4.5 for this domain.
- `electronics/potentiometer-dimmer` — **Issues:** build/test cycle is too linear; instrument-based verification is shallow; fault-isolation branch is missing for bad readings. **Parity upgrades:** Add a branch that handles the most likely failure mode before `finish` unlocks. Gate completion on a mechanics-backed artifact (process output, measured item, or parsed log). Mirror pacing and explanatory depth from checked exemplars in `docs/qa/v3.md` §4.5 for this domain.
- `electronics/resistor-color-check` — **Issues:** build/test cycle is too linear; instrument-based verification is shallow; fault-isolation branch is missing for bad readings. **Parity upgrades:** Add a branch that handles the most likely failure mode before `finish` unlocks. Gate completion on a mechanics-backed artifact (process output, measured item, or parsed log). Mirror pacing and explanatory depth from checked exemplars in `docs/qa/v3.md` §4.5 for this domain.
- `electronics/servo-sweep` — **Issues:** build/test cycle is too linear; instrument-based verification is shallow; fault-isolation branch is missing for bad readings. **Parity upgrades:** Add a branch that handles the most likely failure mode before `finish` unlocks. Gate completion on a mechanics-backed artifact (process output, measured item, or parsed log). Mirror pacing and explanatory depth from checked exemplars in `docs/qa/v3.md` §4.5 for this domain.
- `electronics/solder-led-harness` — **Issues:** build/test cycle is too linear; instrument-based verification is shallow; fault-isolation branch is missing for bad readings. **Parity upgrades:** Add a branch that handles the most likely failure mode before `finish` unlocks. Gate completion on a mechanics-backed artifact (process output, measured item, or parsed log). Mirror pacing and explanatory depth from checked exemplars in `docs/qa/v3.md` §4.5 for this domain.
- `electronics/solder-wire` — **Issues:** build/test cycle is too linear; instrument-based verification is shallow; fault-isolation branch is missing for bad readings. **Parity upgrades:** Add a branch that handles the most likely failure mode before `finish` unlocks. Gate completion on a mechanics-backed artifact (process output, measured item, or parsed log). Mirror pacing and explanatory depth from checked exemplars in `docs/qa/v3.md` §4.5 for this domain.
- `electronics/soldering-intro` — **Issues:** build/test cycle is too linear; instrument-based verification is shallow; fault-isolation branch is missing for bad readings. **Parity upgrades:** Add a branch that handles the most likely failure mode before `finish` unlocks. Gate completion on a mechanics-backed artifact (process output, measured item, or parsed log). Mirror pacing and explanatory depth from checked exemplars in `docs/qa/v3.md` §4.5 for this domain.
- `electronics/temperature-plot` — **Issues:** build/test cycle is too linear; instrument-based verification is shallow; fault-isolation branch is missing for bad readings. **Parity upgrades:** Add a branch that handles the most likely failure mode before `finish` unlocks. Gate completion on a mechanics-backed artifact (process output, measured item, or parsed log). Mirror pacing and explanatory depth from checked exemplars in `docs/qa/v3.md` §4.5 for this domain.
- `electronics/test-gfci-outlet` — **Issues:** build/test cycle is too linear; instrument-based verification is shallow; fault-isolation branch is missing for bad readings. **Parity upgrades:** Add a branch that handles the most likely failure mode before `finish` unlocks. Gate completion on a mechanics-backed artifact (process output, measured item, or parsed log). Mirror pacing and explanatory depth from checked exemplars in `docs/qa/v3.md` §4.5 for this domain.
- `electronics/thermistor-reading` — **Issues:** build/test cycle is too linear; instrument-based verification is shallow; fault-isolation branch is missing for bad readings. **Parity upgrades:** Add a branch that handles the most likely failure mode before `finish` unlocks. Gate completion on a mechanics-backed artifact (process output, measured item, or parsed log). Mirror pacing and explanatory depth from checked exemplars in `docs/qa/v3.md` §4.5 for this domain.
- `electronics/thermometer-calibration` — **Issues:** build/test cycle is too linear; instrument-based verification is shallow; fault-isolation branch is missing for bad readings. **Parity upgrades:** Add a branch that handles the most likely failure mode before `finish` unlocks. Gate completion on a mechanics-backed artifact (process output, measured item, or parsed log). Mirror pacing and explanatory depth from checked exemplars in `docs/qa/v3.md` §4.5 for this domain.
- `electronics/voltage-divider` — **Issues:** build/test cycle is too linear; instrument-based verification is shallow; fault-isolation branch is missing for bad readings. **Parity upgrades:** Add a branch that handles the most likely failure mode before `finish` unlocks. Gate completion on a mechanics-backed artifact (process output, measured item, or parsed log). Mirror pacing and explanatory depth from checked exemplars in `docs/qa/v3.md` §4.5 for this domain.

### energy (11 quests)

- `energy/battery-maintenance` — **Issues:** resource progression leans on numeric grind gates; strategy branches (efficiency vs output) are limited; evidence of sustained operation is under-modeled. **Parity upgrades:** Add a branch that handles the most likely failure mode before `finish` unlocks. Gate completion on a mechanics-backed artifact (process output, measured item, or parsed log). Mirror pacing and explanatory depth from checked exemplars in `docs/qa/v3.md` §4.5 for this domain.
- `energy/battery-upgrade` — **Issues:** resource progression leans on numeric grind gates; strategy branches (efficiency vs output) are limited; evidence of sustained operation is under-modeled. **Parity upgrades:** Add a branch that handles the most likely failure mode before `finish` unlocks. Gate completion on a mechanics-backed artifact (process output, measured item, or parsed log). Mirror pacing and explanatory depth from checked exemplars in `docs/qa/v3.md` §4.5 for this domain.
- `energy/biogas-digester` — **Issues:** resource progression leans on numeric grind gates; strategy branches (efficiency vs output) are limited; evidence of sustained operation is under-modeled. **Parity upgrades:** Add a branch that handles the most likely failure mode before `finish` unlocks. Gate completion on a mechanics-backed artifact (process output, measured item, or parsed log). Mirror pacing and explanatory depth from checked exemplars in `docs/qa/v3.md` §4.5 for this domain.
- `energy/charge-controller-setup` — **Issues:** resource progression leans on numeric grind gates; strategy branches (efficiency vs output) are limited; evidence of sustained operation is under-modeled. **Parity upgrades:** Add a branch that handles the most likely failure mode before `finish` unlocks. Gate completion on a mechanics-backed artifact (process output, measured item, or parsed log). Mirror pacing and explanatory depth from checked exemplars in `docs/qa/v3.md` §4.5 for this domain.
- `energy/dWatt-1e8` — **Issues:** resource progression leans on numeric grind gates; strategy branches (efficiency vs output) are limited; evidence of sustained operation is under-modeled. **Parity upgrades:** Add a branch that handles the most likely failure mode before `finish` unlocks. Gate completion on a mechanics-backed artifact (process output, measured item, or parsed log). Mirror pacing and explanatory depth from checked exemplars in `docs/qa/v3.md` §4.5 for this domain.
- `energy/hand-crank-generator` — **Issues:** resource progression leans on numeric grind gates; strategy branches (efficiency vs output) are limited; evidence of sustained operation is under-modeled. **Parity upgrades:** Add a branch that handles the most likely failure mode before `finish` unlocks. Gate completion on a mechanics-backed artifact (process output, measured item, or parsed log). Mirror pacing and explanatory depth from checked exemplars in `docs/qa/v3.md` §4.5 for this domain.
- `energy/offgrid-charger` — **Issues:** resource progression leans on numeric grind gates; strategy branches (efficiency vs output) are limited; evidence of sustained operation is under-modeled. **Parity upgrades:** Add a branch that handles the most likely failure mode before `finish` unlocks. Gate completion on a mechanics-backed artifact (process output, measured item, or parsed log). Mirror pacing and explanatory depth from checked exemplars in `docs/qa/v3.md` §4.5 for this domain.
- `energy/portable-solar-panel` — **Issues:** resource progression leans on numeric grind gates; strategy branches (efficiency vs output) are limited; evidence of sustained operation is under-modeled. **Parity upgrades:** Add a branch that handles the most likely failure mode before `finish` unlocks. Gate completion on a mechanics-backed artifact (process output, measured item, or parsed log). Mirror pacing and explanatory depth from checked exemplars in `docs/qa/v3.md` §4.5 for this domain.
- `energy/power-inverter` — **Issues:** resource progression leans on numeric grind gates; strategy branches (efficiency vs output) are limited; evidence of sustained operation is under-modeled. **Parity upgrades:** Add a branch that handles the most likely failure mode before `finish` unlocks. Gate completion on a mechanics-backed artifact (process output, measured item, or parsed log). Mirror pacing and explanatory depth from checked exemplars in `docs/qa/v3.md` §4.5 for this domain.
- `energy/solar-tracker` — **Issues:** resource progression leans on numeric grind gates; strategy branches (efficiency vs output) are limited; evidence of sustained operation is under-modeled. **Parity upgrades:** Add a branch that handles the most likely failure mode before `finish` unlocks. Gate completion on a mechanics-backed artifact (process output, measured item, or parsed log). Mirror pacing and explanatory depth from checked exemplars in `docs/qa/v3.md` §4.5 for this domain.
- `energy/wind-turbine` — **Issues:** resource progression leans on numeric grind gates; strategy branches (efficiency vs output) are limited; evidence of sustained operation is under-modeled. **Parity upgrades:** Add a branch that handles the most likely failure mode before `finish` unlocks. Gate completion on a mechanics-backed artifact (process output, measured item, or parsed log). Mirror pacing and explanatory depth from checked exemplars in `docs/qa/v3.md` §4.5 for this domain.

### firstaid (13 quests)

- `firstaid/assemble-kit` — **Issues:** care protocol can be completed without full triage; incorrect-action safeguards are too soft; evidence of safe sequence completion is limited. **Parity upgrades:** Add a branch that handles the most likely failure mode before `finish` unlocks. Gate completion on a mechanics-backed artifact (process output, measured item, or parsed log). Mirror pacing and explanatory depth from checked exemplars in `docs/qa/v3.md` §4.5 for this domain.
- `firstaid/change-bandage` — **Issues:** care protocol can be completed without full triage; incorrect-action safeguards are too soft; evidence of safe sequence completion is limited. **Parity upgrades:** Add a branch that handles the most likely failure mode before `finish` unlocks. Gate completion on a mechanics-backed artifact (process output, measured item, or parsed log). Mirror pacing and explanatory depth from checked exemplars in `docs/qa/v3.md` §4.5 for this domain.
- `firstaid/dispose-bandages` — **Issues:** care protocol can be completed without full triage; incorrect-action safeguards are too soft; evidence of safe sequence completion is limited. **Parity upgrades:** Add a branch that handles the most likely failure mode before `finish` unlocks. Gate completion on a mechanics-backed artifact (process output, measured item, or parsed log). Mirror pacing and explanatory depth from checked exemplars in `docs/qa/v3.md` §4.5 for this domain.
- `firstaid/dispose-expired` — **Issues:** care protocol can be completed without full triage; incorrect-action safeguards are too soft; evidence of safe sequence completion is limited. **Parity upgrades:** Add a branch that handles the most likely failure mode before `finish` unlocks. Gate completion on a mechanics-backed artifact (process output, measured item, or parsed log). Mirror pacing and explanatory depth from checked exemplars in `docs/qa/v3.md` §4.5 for this domain.
- `firstaid/flashlight-battery` — **Issues:** care protocol can be completed without full triage; incorrect-action safeguards are too soft; evidence of safe sequence completion is limited. **Parity upgrades:** Add a branch that handles the most likely failure mode before `finish` unlocks. Gate completion on a mechanics-backed artifact (process output, measured item, or parsed log). Mirror pacing and explanatory depth from checked exemplars in `docs/qa/v3.md` §4.5 for this domain.
- `firstaid/learn-cpr` — **Issues:** care protocol can be completed without full triage; incorrect-action safeguards are too soft; evidence of safe sequence completion is limited. **Parity upgrades:** Add a branch that handles the most likely failure mode before `finish` unlocks. Gate completion on a mechanics-backed artifact (process output, measured item, or parsed log). Mirror pacing and explanatory depth from checked exemplars in `docs/qa/v3.md` §4.5 for this domain.
- `firstaid/remove-splinter` — **Issues:** care protocol can be completed without full triage; incorrect-action safeguards are too soft; evidence of safe sequence completion is limited. **Parity upgrades:** Add a branch that handles the most likely failure mode before `finish` unlocks. Gate completion on a mechanics-backed artifact (process output, measured item, or parsed log). Mirror pacing and explanatory depth from checked exemplars in `docs/qa/v3.md` §4.5 for this domain.
- `firstaid/restock-kit` — **Issues:** care protocol can be completed without full triage; incorrect-action safeguards are too soft; evidence of safe sequence completion is limited. **Parity upgrades:** Add a branch that handles the most likely failure mode before `finish` unlocks. Gate completion on a mechanics-backed artifact (process output, measured item, or parsed log). Mirror pacing and explanatory depth from checked exemplars in `docs/qa/v3.md` §4.5 for this domain.
- `firstaid/sanitize-pocket-mask` — **Issues:** care protocol can be completed without full triage; incorrect-action safeguards are too soft; evidence of safe sequence completion is limited. **Parity upgrades:** Add a branch that handles the most likely failure mode before `finish` unlocks. Gate completion on a mechanics-backed artifact (process output, measured item, or parsed log). Mirror pacing and explanatory depth from checked exemplars in `docs/qa/v3.md` §4.5 for this domain.
- `firstaid/splint-limb` — **Issues:** care protocol can be completed without full triage; incorrect-action safeguards are too soft; evidence of safe sequence completion is limited. **Parity upgrades:** Add a branch that handles the most likely failure mode before `finish` unlocks. Gate completion on a mechanics-backed artifact (process output, measured item, or parsed log). Mirror pacing and explanatory depth from checked exemplars in `docs/qa/v3.md` §4.5 for this domain.
- `firstaid/stop-nosebleed` — **Issues:** care protocol can be completed without full triage; incorrect-action safeguards are too soft; evidence of safe sequence completion is limited. **Parity upgrades:** Add a branch that handles the most likely failure mode before `finish` unlocks. Gate completion on a mechanics-backed artifact (process output, measured item, or parsed log). Mirror pacing and explanatory depth from checked exemplars in `docs/qa/v3.md` §4.5 for this domain.
- `firstaid/treat-burn` — **Issues:** care protocol can be completed without full triage; incorrect-action safeguards are too soft; evidence of safe sequence completion is limited. **Parity upgrades:** Add a branch that handles the most likely failure mode before `finish` unlocks. Gate completion on a mechanics-backed artifact (process output, measured item, or parsed log). Mirror pacing and explanatory depth from checked exemplars in `docs/qa/v3.md` §4.5 for this domain.
- `firstaid/wound-care` — **Issues:** care protocol can be completed without full triage; incorrect-action safeguards are too soft; evidence of safe sequence completion is limited. **Parity upgrades:** Add a branch that handles the most likely failure mode before `finish` unlocks. Gate completion on a mechanics-backed artifact (process output, measured item, or parsed log). Mirror pacing and explanatory depth from checked exemplars in `docs/qa/v3.md` §4.5 for this domain.

### geothermal (15 quests)

- `geothermal/backflush-loop-filter` — **Issues:** sensor operations are one-pass instead of lifecycle-based; before/after telemetry proofs are weak; fault escalation path is under-specified. **Parity upgrades:** Add a branch that handles the most likely failure mode before `finish` unlocks. Gate completion on a mechanics-backed artifact (process output, measured item, or parsed log). Mirror pacing and explanatory depth from checked exemplars in `docs/qa/v3.md` §4.5 for this domain.
- `geothermal/calibrate-ground-sensor` — **Issues:** calibration pass lacks repeatability and drift re-check; before/after telemetry proofs are weak; fault escalation path is under-specified. **Parity upgrades:** Add a two-pass calibration with tolerance thresholds and re-test gate Gate completion on a mechanics-backed artifact (process output, measured item, or parsed log). Mirror pacing and explanatory depth from checked exemplars in `docs/qa/v3.md` §4.5 for this domain.
- `geothermal/check-loop-inlet-temp` — **Issues:** verification step is single-shot without interpretation; before/after telemetry proofs are weak; fault escalation path is under-specified. **Parity upgrades:** Add acceptable-range evaluation plus remediation branch before finish Gate completion on a mechanics-backed artifact (process output, measured item, or parsed log). Mirror pacing and explanatory depth from checked exemplars in `docs/qa/v3.md` §4.5 for this domain.
- `geothermal/check-loop-outlet-temp` — **Issues:** verification step is single-shot without interpretation; before/after telemetry proofs are weak; fault escalation path is under-specified. **Parity upgrades:** Add acceptable-range evaluation plus remediation branch before finish Gate completion on a mechanics-backed artifact (process output, measured item, or parsed log). Mirror pacing and explanatory depth from checked exemplars in `docs/qa/v3.md` §4.5 for this domain.
- `geothermal/check-loop-pressure` — **Issues:** verification step is single-shot without interpretation; before/after telemetry proofs are weak; fault escalation path is under-specified. **Parity upgrades:** Add acceptable-range evaluation plus remediation branch before finish Gate completion on a mechanics-backed artifact (process output, measured item, or parsed log). Mirror pacing and explanatory depth from checked exemplars in `docs/qa/v3.md` §4.5 for this domain.
- `geothermal/check-loop-temp-delta` — **Issues:** verification step is single-shot without interpretation; before/after telemetry proofs are weak; fault escalation path is under-specified. **Parity upgrades:** Add acceptable-range evaluation plus remediation branch before finish Gate completion on a mechanics-backed artifact (process output, measured item, or parsed log). Mirror pacing and explanatory depth from checked exemplars in `docs/qa/v3.md` §4.5 for this domain.
- `geothermal/compare-depth-ground-temps` — **Issues:** sensor operations are one-pass instead of lifecycle-based; before/after telemetry proofs are weak; fault escalation path is under-specified. **Parity upgrades:** Add a branch that handles the most likely failure mode before `finish` unlocks. Gate completion on a mechanics-backed artifact (process output, measured item, or parsed log). Mirror pacing and explanatory depth from checked exemplars in `docs/qa/v3.md` §4.5 for this domain.
- `geothermal/compare-seasonal-ground-temps` — **Issues:** sensor operations are one-pass instead of lifecycle-based; before/after telemetry proofs are weak; fault escalation path is under-specified. **Parity upgrades:** Add a branch that handles the most likely failure mode before `finish` unlocks. Gate completion on a mechanics-backed artifact (process output, measured item, or parsed log). Mirror pacing and explanatory depth from checked exemplars in `docs/qa/v3.md` §4.5 for this domain.
- `geothermal/install-backup-thermistor` — **Issues:** installation flow lacks verification/rollback; before/after telemetry proofs are weak; fault escalation path is under-specified. **Parity upgrades:** Split install→verify→rollback with distinct gates and outputs Gate completion on a mechanics-backed artifact (process output, measured item, or parsed log). Mirror pacing and explanatory depth from checked exemplars in `docs/qa/v3.md` §4.5 for this domain.
- `geothermal/log-ground-temperature` — **Issues:** logging objective lacks acceptance criteria; before/after telemetry proofs are weak; fault escalation path is under-specified. **Parity upgrades:** Add required log artifact + interpretation node that gates completion Gate completion on a mechanics-backed artifact (process output, measured item, or parsed log). Mirror pacing and explanatory depth from checked exemplars in `docs/qa/v3.md` §4.5 for this domain.
- `geothermal/log-heat-pump-warmup` — **Issues:** logging objective lacks acceptance criteria; before/after telemetry proofs are weak; fault escalation path is under-specified. **Parity upgrades:** Add required log artifact + interpretation node that gates completion Gate completion on a mechanics-backed artifact (process output, measured item, or parsed log). Mirror pacing and explanatory depth from checked exemplars in `docs/qa/v3.md` §4.5 for this domain.
- `geothermal/monitor-heat-pump-energy` — **Issues:** monitoring objective lacks anomaly response; before/after telemetry proofs are weak; fault escalation path is under-specified. **Parity upgrades:** Add anomaly branch with rollback/escalation and proof artifact Gate completion on a mechanics-backed artifact (process output, measured item, or parsed log). Mirror pacing and explanatory depth from checked exemplars in `docs/qa/v3.md` §4.5 for this domain.
- `geothermal/purge-loop-air` — **Issues:** sensor operations are one-pass instead of lifecycle-based; before/after telemetry proofs are weak; fault escalation path is under-specified. **Parity upgrades:** Add a branch that handles the most likely failure mode before `finish` unlocks. Gate completion on a mechanics-backed artifact (process output, measured item, or parsed log). Mirror pacing and explanatory depth from checked exemplars in `docs/qa/v3.md` §4.5 for this domain.
- `geothermal/replace-faulty-thermistor` — **Issues:** sensor operations are one-pass instead of lifecycle-based; before/after telemetry proofs are weak; fault escalation path is under-specified. **Parity upgrades:** Add a branch that handles the most likely failure mode before `finish` unlocks. Gate completion on a mechanics-backed artifact (process output, measured item, or parsed log). Mirror pacing and explanatory depth from checked exemplars in `docs/qa/v3.md` §4.5 for this domain.
- `geothermal/survey-ground-temperature` — **Issues:** sensor operations are one-pass instead of lifecycle-based; before/after telemetry proofs are weak; fault escalation path is under-specified. **Parity upgrades:** Add a branch that handles the most likely failure mode before `finish` unlocks. Gate completion on a mechanics-backed artifact (process output, measured item, or parsed log). Mirror pacing and explanatory depth from checked exemplars in `docs/qa/v3.md` §4.5 for this domain.

### hydroponics (20 quests)

- `hydroponics/air-stone-soak` — **Issues:** maintenance loops are brief and repetitive; measurement-to-action branching is thin; contamination/recovery flow is underdeveloped. **Parity upgrades:** Add a branch that handles the most likely failure mode before `finish` unlocks. Gate completion on a mechanics-backed artifact (process output, measured item, or parsed log). Mirror pacing and explanatory depth from checked exemplars in `docs/qa/v3.md` §4.5 for this domain.
- `hydroponics/ec-calibrate` — **Issues:** calibration pass lacks repeatability and drift re-check; measurement-to-action branching is thin; contamination/recovery flow is underdeveloped. **Parity upgrades:** Add a two-pass calibration with tolerance thresholds and re-test gate Gate completion on a mechanics-backed artifact (process output, measured item, or parsed log). Mirror pacing and explanatory depth from checked exemplars in `docs/qa/v3.md` §4.5 for this domain.
- `hydroponics/ec-check` — **Issues:** maintenance loops are brief and repetitive; measurement-to-action branching is thin; contamination/recovery flow is underdeveloped. **Parity upgrades:** Add a branch that handles the most likely failure mode before `finish` unlocks. Gate completion on a mechanics-backed artifact (process output, measured item, or parsed log). Mirror pacing and explanatory depth from checked exemplars in `docs/qa/v3.md` §4.5 for this domain.
- `hydroponics/filter-clean` — **Issues:** maintenance loops are brief and repetitive; measurement-to-action branching is thin; contamination/recovery flow is underdeveloped. **Parity upgrades:** Add a branch that handles the most likely failure mode before `finish` unlocks. Gate completion on a mechanics-backed artifact (process output, measured item, or parsed log). Mirror pacing and explanatory depth from checked exemplars in `docs/qa/v3.md` §4.5 for this domain.
- `hydroponics/grow-light` — **Issues:** maintenance loops are brief and repetitive; measurement-to-action branching is thin; contamination/recovery flow is underdeveloped. **Parity upgrades:** Add a branch that handles the most likely failure mode before `finish` unlocks. Gate completion on a mechanics-backed artifact (process output, measured item, or parsed log). Mirror pacing and explanatory depth from checked exemplars in `docs/qa/v3.md` §4.5 for this domain.
- `hydroponics/lettuce` — **Issues:** maintenance loops are brief and repetitive; measurement-to-action branching is thin; contamination/recovery flow is underdeveloped. **Parity upgrades:** Add a branch that handles the most likely failure mode before `finish` unlocks. Gate completion on a mechanics-backed artifact (process output, measured item, or parsed log). Mirror pacing and explanatory depth from checked exemplars in `docs/qa/v3.md` §4.5 for this domain.
- `hydroponics/mint-cutting` — **Issues:** maintenance loops are brief and repetitive; measurement-to-action branching is thin; contamination/recovery flow is underdeveloped. **Parity upgrades:** Add a branch that handles the most likely failure mode before `finish` unlocks. Gate completion on a mechanics-backed artifact (process output, measured item, or parsed log). Mirror pacing and explanatory depth from checked exemplars in `docs/qa/v3.md` §4.5 for this domain.
- `hydroponics/netcup-clean` — **Issues:** maintenance loops are brief and repetitive; measurement-to-action branching is thin; contamination/recovery flow is underdeveloped. **Parity upgrades:** Add a branch that handles the most likely failure mode before `finish` unlocks. Gate completion on a mechanics-backed artifact (process output, measured item, or parsed log). Mirror pacing and explanatory depth from checked exemplars in `docs/qa/v3.md` §4.5 for this domain.
- `hydroponics/ph-check` — **Issues:** maintenance loops are brief and repetitive; measurement-to-action branching is thin; contamination/recovery flow is underdeveloped. **Parity upgrades:** Add a branch that handles the most likely failure mode before `finish` unlocks. Gate completion on a mechanics-backed artifact (process output, measured item, or parsed log). Mirror pacing and explanatory depth from checked exemplars in `docs/qa/v3.md` §4.5 for this domain.
- `hydroponics/ph-test` — **Issues:** maintenance loops are brief and repetitive; measurement-to-action branching is thin; contamination/recovery flow is underdeveloped. **Parity upgrades:** Add a branch that handles the most likely failure mode before `finish` unlocks. Gate completion on a mechanics-backed artifact (process output, measured item, or parsed log). Mirror pacing and explanatory depth from checked exemplars in `docs/qa/v3.md` §4.5 for this domain.
- `hydroponics/plug-soak` — **Issues:** maintenance loops are brief and repetitive; measurement-to-action branching is thin; contamination/recovery flow is underdeveloped. **Parity upgrades:** Add a branch that handles the most likely failure mode before `finish` unlocks. Gate completion on a mechanics-backed artifact (process output, measured item, or parsed log). Mirror pacing and explanatory depth from checked exemplars in `docs/qa/v3.md` §4.5 for this domain.
- `hydroponics/pump-install` — **Issues:** installation flow lacks verification/rollback; measurement-to-action branching is thin; contamination/recovery flow is underdeveloped. **Parity upgrades:** Split install→verify→rollback with distinct gates and outputs Gate completion on a mechanics-backed artifact (process output, measured item, or parsed log). Mirror pacing and explanatory depth from checked exemplars in `docs/qa/v3.md` §4.5 for this domain.
- `hydroponics/pump-prime` — **Issues:** maintenance loops are brief and repetitive; measurement-to-action branching is thin; contamination/recovery flow is underdeveloped. **Parity upgrades:** Add a branch that handles the most likely failure mode before `finish` unlocks. Gate completion on a mechanics-backed artifact (process output, measured item, or parsed log). Mirror pacing and explanatory depth from checked exemplars in `docs/qa/v3.md` §4.5 for this domain.
- `hydroponics/regrow-stevia` — **Issues:** maintenance loops are brief and repetitive; measurement-to-action branching is thin; contamination/recovery flow is underdeveloped. **Parity upgrades:** Add a branch that handles the most likely failure mode before `finish` unlocks. Gate completion on a mechanics-backed artifact (process output, measured item, or parsed log). Mirror pacing and explanatory depth from checked exemplars in `docs/qa/v3.md` §4.5 for this domain.
- `hydroponics/reservoir-refresh` — **Issues:** maintenance loops are brief and repetitive; measurement-to-action branching is thin; contamination/recovery flow is underdeveloped. **Parity upgrades:** Add a branch that handles the most likely failure mode before `finish` unlocks. Gate completion on a mechanics-backed artifact (process output, measured item, or parsed log). Mirror pacing and explanatory depth from checked exemplars in `docs/qa/v3.md` §4.5 for this domain.
- `hydroponics/root-rinse` — **Issues:** maintenance loops are brief and repetitive; measurement-to-action branching is thin; contamination/recovery flow is underdeveloped. **Parity upgrades:** Add a branch that handles the most likely failure mode before `finish` unlocks. Gate completion on a mechanics-backed artifact (process output, measured item, or parsed log). Mirror pacing and explanatory depth from checked exemplars in `docs/qa/v3.md` §4.5 for this domain.
- `hydroponics/stevia` — **Issues:** maintenance loops are brief and repetitive; measurement-to-action branching is thin; contamination/recovery flow is underdeveloped. **Parity upgrades:** Add a branch that handles the most likely failure mode before `finish` unlocks. Gate completion on a mechanics-backed artifact (process output, measured item, or parsed log). Mirror pacing and explanatory depth from checked exemplars in `docs/qa/v3.md` §4.5 for this domain.
- `hydroponics/temp-check` — **Issues:** maintenance loops are brief and repetitive; measurement-to-action branching is thin; contamination/recovery flow is underdeveloped. **Parity upgrades:** Add a branch that handles the most likely failure mode before `finish` unlocks. Gate completion on a mechanics-backed artifact (process output, measured item, or parsed log). Mirror pacing and explanatory depth from checked exemplars in `docs/qa/v3.md` §4.5 for this domain.
- `hydroponics/top-off` — **Issues:** maintenance loops are brief and repetitive; measurement-to-action branching is thin; contamination/recovery flow is underdeveloped. **Parity upgrades:** Add a branch that handles the most likely failure mode before `finish` unlocks. Gate completion on a mechanics-backed artifact (process output, measured item, or parsed log). Mirror pacing and explanatory depth from checked exemplars in `docs/qa/v3.md` §4.5 for this domain.
- `hydroponics/tub-scrub` — **Issues:** maintenance loops are brief and repetitive; measurement-to-action branching is thin; contamination/recovery flow is underdeveloped. **Parity upgrades:** Add a branch that handles the most likely failure mode before `finish` unlocks. Gate completion on a mechanics-backed artifact (process output, measured item, or parsed log). Mirror pacing and explanatory depth from checked exemplars in `docs/qa/v3.md` §4.5 for this domain.

### programming (18 quests)

- `programming/avg-temp` — **Issues:** code-delivery path skips debug/verification depth; test/log artifacts are optional or absent; error-handling branches are underspecified. **Parity upgrades:** Add a branch that handles the most likely failure mode before `finish` unlocks. Gate completion on a mechanics-backed artifact (process output, measured item, or parsed log). Mirror pacing and explanatory depth from checked exemplars in `docs/qa/v3.md` §4.5 for this domain.
- `programming/graph-temp` — **Issues:** code-delivery path skips debug/verification depth; test/log artifacts are optional or absent; error-handling branches are underspecified. **Parity upgrades:** Add a branch that handles the most likely failure mode before `finish` unlocks. Gate completion on a mechanics-backed artifact (process output, measured item, or parsed log). Mirror pacing and explanatory depth from checked exemplars in `docs/qa/v3.md` §4.5 for this domain.
- `programming/graph-temp-data` — **Issues:** code-delivery path skips debug/verification depth; test/log artifacts are optional or absent; error-handling branches are underspecified. **Parity upgrades:** Add a branch that handles the most likely failure mode before `finish` unlocks. Gate completion on a mechanics-backed artifact (process output, measured item, or parsed log). Mirror pacing and explanatory depth from checked exemplars in `docs/qa/v3.md` §4.5 for this domain.
- `programming/hello-sensor` — **Issues:** code-delivery path skips debug/verification depth; test/log artifacts are optional or absent; error-handling branches are underspecified. **Parity upgrades:** Add a branch that handles the most likely failure mode before `finish` unlocks. Gate completion on a mechanics-backed artifact (process output, measured item, or parsed log). Mirror pacing and explanatory depth from checked exemplars in `docs/qa/v3.md` §4.5 for this domain.
- `programming/http-post` — **Issues:** code-delivery path skips debug/verification depth; test/log artifacts are optional or absent; error-handling branches are underspecified. **Parity upgrades:** Add a branch that handles the most likely failure mode before `finish` unlocks. Gate completion on a mechanics-backed artifact (process output, measured item, or parsed log). Mirror pacing and explanatory depth from checked exemplars in `docs/qa/v3.md` §4.5 for this domain.
- `programming/json-api` — **Issues:** code-delivery path skips debug/verification depth; test/log artifacts are optional or absent; error-handling branches are underspecified. **Parity upgrades:** Add a branch that handles the most likely failure mode before `finish` unlocks. Gate completion on a mechanics-backed artifact (process output, measured item, or parsed log). Mirror pacing and explanatory depth from checked exemplars in `docs/qa/v3.md` §4.5 for this domain.
- `programming/json-endpoint` — **Issues:** code-delivery path skips debug/verification depth; test/log artifacts are optional or absent; error-handling branches are underspecified. **Parity upgrades:** Add a branch that handles the most likely failure mode before `finish` unlocks. Gate completion on a mechanics-backed artifact (process output, measured item, or parsed log). Mirror pacing and explanatory depth from checked exemplars in `docs/qa/v3.md` §4.5 for this domain.
- `programming/median-temp` — **Issues:** code-delivery path skips debug/verification depth; test/log artifacts are optional or absent; error-handling branches are underspecified. **Parity upgrades:** Add a branch that handles the most likely failure mode before `finish` unlocks. Gate completion on a mechanics-backed artifact (process output, measured item, or parsed log). Mirror pacing and explanatory depth from checked exemplars in `docs/qa/v3.md` §4.5 for this domain.
- `programming/moving-avg-temp` — **Issues:** code-delivery path skips debug/verification depth; test/log artifacts are optional or absent; error-handling branches are underspecified. **Parity upgrades:** Add a branch that handles the most likely failure mode before `finish` unlocks. Gate completion on a mechanics-backed artifact (process output, measured item, or parsed log). Mirror pacing and explanatory depth from checked exemplars in `docs/qa/v3.md` §4.5 for this domain.
- `programming/plot-temp-cli` — **Issues:** code-delivery path skips debug/verification depth; test/log artifacts are optional or absent; error-handling branches are underspecified. **Parity upgrades:** Add a branch that handles the most likely failure mode before `finish` unlocks. Gate completion on a mechanics-backed artifact (process output, measured item, or parsed log). Mirror pacing and explanatory depth from checked exemplars in `docs/qa/v3.md` §4.5 for this domain.
- `programming/stddev-temp` — **Issues:** code-delivery path skips debug/verification depth; test/log artifacts are optional or absent; error-handling branches are underspecified. **Parity upgrades:** Add a branch that handles the most likely failure mode before `finish` unlocks. Gate completion on a mechanics-backed artifact (process output, measured item, or parsed log). Mirror pacing and explanatory depth from checked exemplars in `docs/qa/v3.md` §4.5 for this domain.
- `programming/temp-alert` — **Issues:** code-delivery path skips debug/verification depth; test/log artifacts are optional or absent; error-handling branches are underspecified. **Parity upgrades:** Add a branch that handles the most likely failure mode before `finish` unlocks. Gate completion on a mechanics-backed artifact (process output, measured item, or parsed log). Mirror pacing and explanatory depth from checked exemplars in `docs/qa/v3.md` §4.5 for this domain.
- `programming/temp-email` — **Issues:** code-delivery path skips debug/verification depth; test/log artifacts are optional or absent; error-handling branches are underspecified. **Parity upgrades:** Add a branch that handles the most likely failure mode before `finish` unlocks. Gate completion on a mechanics-backed artifact (process output, measured item, or parsed log). Mirror pacing and explanatory depth from checked exemplars in `docs/qa/v3.md` §4.5 for this domain.
- `programming/temp-graph` — **Issues:** code-delivery path skips debug/verification depth; test/log artifacts are optional or absent; error-handling branches are underspecified. **Parity upgrades:** Add a branch that handles the most likely failure mode before `finish` unlocks. Gate completion on a mechanics-backed artifact (process output, measured item, or parsed log). Mirror pacing and explanatory depth from checked exemplars in `docs/qa/v3.md` §4.5 for this domain.
- `programming/temp-json-api` — **Issues:** code-delivery path skips debug/verification depth; test/log artifacts are optional or absent; error-handling branches are underspecified. **Parity upgrades:** Add a branch that handles the most likely failure mode before `finish` unlocks. Gate completion on a mechanics-backed artifact (process output, measured item, or parsed log). Mirror pacing and explanatory depth from checked exemplars in `docs/qa/v3.md` §4.5 for this domain.
- `programming/temp-logger` — **Issues:** code-delivery path skips debug/verification depth; test/log artifacts are optional or absent; error-handling branches are underspecified. **Parity upgrades:** Add a branch that handles the most likely failure mode before `finish` unlocks. Gate completion on a mechanics-backed artifact (process output, measured item, or parsed log). Mirror pacing and explanatory depth from checked exemplars in `docs/qa/v3.md` §4.5 for this domain.
- `programming/thermistor-calibration` — **Issues:** code-delivery path skips debug/verification depth; test/log artifacts are optional or absent; error-handling branches are underspecified. **Parity upgrades:** Add a branch that handles the most likely failure mode before `finish` unlocks. Gate completion on a mechanics-backed artifact (process output, measured item, or parsed log). Mirror pacing and explanatory depth from checked exemplars in `docs/qa/v3.md` §4.5 for this domain.
- `programming/web-server` — **Issues:** code-delivery path skips debug/verification depth; test/log artifacts are optional or absent; error-handling branches are underspecified. **Parity upgrades:** Add a branch that handles the most likely failure mode before `finish` unlocks. Gate completion on a mechanics-backed artifact (process output, measured item, or parsed log). Mirror pacing and explanatory depth from checked exemplars in `docs/qa/v3.md` §4.5 for this domain.

### robotics (13 quests)

- `robotics/gyro-balance` — **Issues:** control loop calibration depth is limited; telemetry/benchmark evidence gate is weak; recovery behavior for instability is missing. **Parity upgrades:** Add a branch that handles the most likely failure mode before `finish` unlocks. Gate completion on a mechanics-backed artifact (process output, measured item, or parsed log). Mirror pacing and explanatory depth from checked exemplars in `docs/qa/v3.md` §4.5 for this domain.
- `robotics/line-follower` — **Issues:** control loop calibration depth is limited; telemetry/benchmark evidence gate is weak; recovery behavior for instability is missing. **Parity upgrades:** Add a branch that handles the most likely failure mode before `finish` unlocks. Gate completion on a mechanics-backed artifact (process output, measured item, or parsed log). Mirror pacing and explanatory depth from checked exemplars in `docs/qa/v3.md` §4.5 for this domain.
- `robotics/maze-navigation` — **Issues:** control loop calibration depth is limited; telemetry/benchmark evidence gate is weak; recovery behavior for instability is missing. **Parity upgrades:** Add a branch that handles the most likely failure mode before `finish` unlocks. Gate completion on a mechanics-backed artifact (process output, measured item, or parsed log). Mirror pacing and explanatory depth from checked exemplars in `docs/qa/v3.md` §4.5 for this domain.
- `robotics/obstacle-avoidance` — **Issues:** control loop calibration depth is limited; telemetry/benchmark evidence gate is weak; recovery behavior for instability is missing. **Parity upgrades:** Add a branch that handles the most likely failure mode before `finish` unlocks. Gate completion on a mechanics-backed artifact (process output, measured item, or parsed log). Mirror pacing and explanatory depth from checked exemplars in `docs/qa/v3.md` §4.5 for this domain.
- `robotics/odometry-basics` — **Issues:** control loop calibration depth is limited; telemetry/benchmark evidence gate is weak; recovery behavior for instability is missing. **Parity upgrades:** Add a branch that handles the most likely failure mode before `finish` unlocks. Gate completion on a mechanics-backed artifact (process output, measured item, or parsed log). Mirror pacing and explanatory depth from checked exemplars in `docs/qa/v3.md` §4.5 for this domain.
- `robotics/pan-tilt` — **Issues:** control loop calibration depth is limited; telemetry/benchmark evidence gate is weak; recovery behavior for instability is missing. **Parity upgrades:** Add a branch that handles the most likely failure mode before `finish` unlocks. Gate completion on a mechanics-backed artifact (process output, measured item, or parsed log). Mirror pacing and explanatory depth from checked exemplars in `docs/qa/v3.md` §4.5 for this domain.
- `robotics/reflectance-sensors` — **Issues:** control loop calibration depth is limited; telemetry/benchmark evidence gate is weak; recovery behavior for instability is missing. **Parity upgrades:** Add a branch that handles the most likely failure mode before `finish` unlocks. Gate completion on a mechanics-backed artifact (process output, measured item, or parsed log). Mirror pacing and explanatory depth from checked exemplars in `docs/qa/v3.md` §4.5 for this domain.
- `robotics/servo-arm` — **Issues:** control loop calibration depth is limited; telemetry/benchmark evidence gate is weak; recovery behavior for instability is missing. **Parity upgrades:** Add a branch that handles the most likely failure mode before `finish` unlocks. Gate completion on a mechanics-backed artifact (process output, measured item, or parsed log). Mirror pacing and explanatory depth from checked exemplars in `docs/qa/v3.md` §4.5 for this domain.
- `robotics/servo-control` — **Issues:** control loop calibration depth is limited; telemetry/benchmark evidence gate is weak; recovery behavior for instability is missing. **Parity upgrades:** Add a branch that handles the most likely failure mode before `finish` unlocks. Gate completion on a mechanics-backed artifact (process output, measured item, or parsed log). Mirror pacing and explanatory depth from checked exemplars in `docs/qa/v3.md` §4.5 for this domain.
- `robotics/servo-gripper` — **Issues:** control loop calibration depth is limited; telemetry/benchmark evidence gate is weak; recovery behavior for instability is missing. **Parity upgrades:** Add a branch that handles the most likely failure mode before `finish` unlocks. Gate completion on a mechanics-backed artifact (process output, measured item, or parsed log). Mirror pacing and explanatory depth from checked exemplars in `docs/qa/v3.md` §4.5 for this domain.
- `robotics/servo-radar` — **Issues:** control loop calibration depth is limited; telemetry/benchmark evidence gate is weak; recovery behavior for instability is missing. **Parity upgrades:** Add a branch that handles the most likely failure mode before `finish` unlocks. Gate completion on a mechanics-backed artifact (process output, measured item, or parsed log). Mirror pacing and explanatory depth from checked exemplars in `docs/qa/v3.md` §4.5 for this domain.
- `robotics/ultrasonic-rangefinder` — **Issues:** control loop calibration depth is limited; telemetry/benchmark evidence gate is weak; recovery behavior for instability is missing. **Parity upgrades:** Add a branch that handles the most likely failure mode before `finish` unlocks. Gate completion on a mechanics-backed artifact (process output, measured item, or parsed log). Mirror pacing and explanatory depth from checked exemplars in `docs/qa/v3.md` §4.5 for this domain.
- `robotics/wheel-encoders` — **Issues:** control loop calibration depth is limited; telemetry/benchmark evidence gate is weak; recovery behavior for instability is missing. **Parity upgrades:** Add a branch that handles the most likely failure mode before `finish` unlocks. Gate completion on a mechanics-backed artifact (process output, measured item, or parsed log). Mirror pacing and explanatory depth from checked exemplars in `docs/qa/v3.md` §4.5 for this domain.

### rocketry (8 quests)

- `rocketry/fuel-mixture` — **Issues:** mission flow compresses preparation and verification; safety go/no-go checks need stronger gating; post-run analysis branch is minimal. **Parity upgrades:** Add a branch that handles the most likely failure mode before `finish` unlocks. Gate completion on a mechanics-backed artifact (process output, measured item, or parsed log). Mirror pacing and explanatory depth from checked exemplars in `docs/qa/v3.md` §4.5 for this domain.
- `rocketry/guided-rocket-build` — **Issues:** mission flow compresses preparation and verification; safety go/no-go checks need stronger gating; post-run analysis branch is minimal. **Parity upgrades:** Add a branch that handles the most likely failure mode before `finish` unlocks. Gate completion on a mechanics-backed artifact (process output, measured item, or parsed log). Mirror pacing and explanatory depth from checked exemplars in `docs/qa/v3.md` §4.5 for this domain.
- `rocketry/night-launch` — **Issues:** mission flow compresses preparation and verification; safety go/no-go checks need stronger gating; post-run analysis branch is minimal. **Parity upgrades:** Add a branch that handles the most likely failure mode before `finish` unlocks. Gate completion on a mechanics-backed artifact (process output, measured item, or parsed log). Mirror pacing and explanatory depth from checked exemplars in `docs/qa/v3.md` §4.5 for this domain.
- `rocketry/preflight-check` — **Issues:** mission flow compresses preparation and verification; safety go/no-go checks need stronger gating; post-run analysis branch is minimal. **Parity upgrades:** Add a branch that handles the most likely failure mode before `finish` unlocks. Gate completion on a mechanics-backed artifact (process output, measured item, or parsed log). Mirror pacing and explanatory depth from checked exemplars in `docs/qa/v3.md` §4.5 for this domain.
- `rocketry/recovery-run` — **Issues:** mission flow compresses preparation and verification; safety go/no-go checks need stronger gating; post-run analysis branch is minimal. **Parity upgrades:** Add a branch that handles the most likely failure mode before `finish` unlocks. Gate completion on a mechanics-backed artifact (process output, measured item, or parsed log). Mirror pacing and explanatory depth from checked exemplars in `docs/qa/v3.md` §4.5 for this domain.
- `rocketry/static-test` — **Issues:** mission flow compresses preparation and verification; safety go/no-go checks need stronger gating; post-run analysis branch is minimal. **Parity upgrades:** Add a branch that handles the most likely failure mode before `finish` unlocks. Gate completion on a mechanics-backed artifact (process output, measured item, or parsed log). Mirror pacing and explanatory depth from checked exemplars in `docs/qa/v3.md` §4.5 for this domain.
- `rocketry/suborbital-hop` — **Issues:** mission flow compresses preparation and verification; safety go/no-go checks need stronger gating; post-run analysis branch is minimal. **Parity upgrades:** Add a branch that handles the most likely failure mode before `finish` unlocks. Gate completion on a mechanics-backed artifact (process output, measured item, or parsed log). Mirror pacing and explanatory depth from checked exemplars in `docs/qa/v3.md` §4.5 for this domain.
- `rocketry/wind-check` — **Issues:** mission flow compresses preparation and verification; safety go/no-go checks need stronger gating; post-run analysis branch is minimal. **Parity upgrades:** Add a branch that handles the most likely failure mode before `finish` unlocks. Gate completion on a mechanics-backed artifact (process output, measured item, or parsed log). Mirror pacing and explanatory depth from checked exemplars in `docs/qa/v3.md` §4.5 for this domain.

### ubi (3 quests)

- `ubi/first-payment` — **Issues:** economy loop progression is overly linear; budget tradeoff decisions are thin; longitudinal proof of behavior change is weak. **Parity upgrades:** Add a branch that handles the most likely failure mode before `finish` unlocks. Gate completion on a mechanics-backed artifact (process output, measured item, or parsed log). Mirror pacing and explanatory depth from checked exemplars in `docs/qa/v3.md` §4.5 for this domain.
- `ubi/reminder` — **Issues:** economy loop progression is overly linear; budget tradeoff decisions are thin; longitudinal proof of behavior change is weak. **Parity upgrades:** Add a branch that handles the most likely failure mode before `finish` unlocks. Gate completion on a mechanics-backed artifact (process output, measured item, or parsed log). Mirror pacing and explanatory depth from checked exemplars in `docs/qa/v3.md` §4.5 for this domain.
- `ubi/savings-goal` — **Issues:** economy loop progression is overly linear; budget tradeoff decisions are thin; longitudinal proof of behavior change is weak. **Parity upgrades:** Add a branch that handles the most likely failure mode before `finish` unlocks. Gate completion on a mechanics-backed artifact (process output, measured item, or parsed log). Mirror pacing and explanatory depth from checked exemplars in `docs/qa/v3.md` §4.5 for this domain.

### woodworking (10 quests)

- `woodworking/apply-finish` — **Issues:** fabrication sequence ends without tolerance validation; quality-control evidence steps are sparse; repair/rework branch for defects is missing. **Parity upgrades:** Add a branch that handles the most likely failure mode before `finish` unlocks. Gate completion on a mechanics-backed artifact (process output, measured item, or parsed log). Mirror pacing and explanatory depth from checked exemplars in `docs/qa/v3.md` §4.5 for this domain.
- `woodworking/birdhouse` — **Issues:** fabrication sequence ends without tolerance validation; quality-control evidence steps are sparse; repair/rework branch for defects is missing. **Parity upgrades:** Add a branch that handles the most likely failure mode before `finish` unlocks. Gate completion on a mechanics-backed artifact (process output, measured item, or parsed log). Mirror pacing and explanatory depth from checked exemplars in `docs/qa/v3.md` §4.5 for this domain.
- `woodworking/bookshelf` — **Issues:** fabrication sequence ends without tolerance validation; quality-control evidence steps are sparse; repair/rework branch for defects is missing. **Parity upgrades:** Add a branch that handles the most likely failure mode before `finish` unlocks. Gate completion on a mechanics-backed artifact (process output, measured item, or parsed log). Mirror pacing and explanatory depth from checked exemplars in `docs/qa/v3.md` §4.5 for this domain.
- `woodworking/coffee-table` — **Issues:** fabrication sequence ends without tolerance validation; quality-control evidence steps are sparse; repair/rework branch for defects is missing. **Parity upgrades:** Add a branch that handles the most likely failure mode before `finish` unlocks. Gate completion on a mechanics-backed artifact (process output, measured item, or parsed log). Mirror pacing and explanatory depth from checked exemplars in `docs/qa/v3.md` §4.5 for this domain.
- `woodworking/finish-sanding` — **Issues:** fabrication sequence ends without tolerance validation; quality-control evidence steps are sparse; repair/rework branch for defects is missing. **Parity upgrades:** Add a branch that handles the most likely failure mode before `finish` unlocks. Gate completion on a mechanics-backed artifact (process output, measured item, or parsed log). Mirror pacing and explanatory depth from checked exemplars in `docs/qa/v3.md` §4.5 for this domain.
- `woodworking/picture-frame` — **Issues:** fabrication sequence ends without tolerance validation; quality-control evidence steps are sparse; repair/rework branch for defects is missing. **Parity upgrades:** Add a branch that handles the most likely failure mode before `finish` unlocks. Gate completion on a mechanics-backed artifact (process output, measured item, or parsed log). Mirror pacing and explanatory depth from checked exemplars in `docs/qa/v3.md` §4.5 for this domain.
- `woodworking/planter-box` — **Issues:** fabrication sequence ends without tolerance validation; quality-control evidence steps are sparse; repair/rework branch for defects is missing. **Parity upgrades:** Add a branch that handles the most likely failure mode before `finish` unlocks. Gate completion on a mechanics-backed artifact (process output, measured item, or parsed log). Mirror pacing and explanatory depth from checked exemplars in `docs/qa/v3.md` §4.5 for this domain.
- `woodworking/step-stool` — **Issues:** fabrication sequence ends without tolerance validation; quality-control evidence steps are sparse; repair/rework branch for defects is missing. **Parity upgrades:** Add a branch that handles the most likely failure mode before `finish` unlocks. Gate completion on a mechanics-backed artifact (process output, measured item, or parsed log). Mirror pacing and explanatory depth from checked exemplars in `docs/qa/v3.md` §4.5 for this domain.
- `woodworking/tool-rack` — **Issues:** fabrication sequence ends without tolerance validation; quality-control evidence steps are sparse; repair/rework branch for defects is missing. **Parity upgrades:** Add a branch that handles the most likely failure mode before `finish` unlocks. Gate completion on a mechanics-backed artifact (process output, measured item, or parsed log). Mirror pacing and explanatory depth from checked exemplars in `docs/qa/v3.md` §4.5 for this domain.
- `woodworking/workbench` — **Issues:** fabrication sequence ends without tolerance validation; quality-control evidence steps are sparse; repair/rework branch for defects is missing. **Parity upgrades:** Add a branch that handles the most likely failure mode before `finish` unlocks. Gate completion on a mechanics-backed artifact (process output, measured item, or parsed log). Mirror pacing and explanatory depth from checked exemplars in `docs/qa/v3.md` §4.5 for this domain.

## Documentation improvements needed for quest authoring

These improvements are meant for what humans/agents read right after `AGENTS.md` references prompt
docs.

### A) Add a “quality bar” section to quest prompts

Add hard requirements beyond schema validity:

- Minimum interaction depth expectation (or explicit rationale for short quests).
- At least one troubleshooting/recovery branch for technical quests.
- At least one mechanics-backed evidence gate (not only prose confirmation).
- Domain-specific safety/realism checks when applicable.

### B) Add anti-pattern lint guidance for prompt users

Authoring docs should explicitly call out and forbid common shallow patterns:

- “Three-node thin shell” quests unless intentionally marked as micro-quests.
- Pure accumulation ladders without strategic decisions.
- Hardening score inflation not backed by substantive quest revisions.

### C) Add a pre-commit self-review checklist for humans + LLMs

Before committing a quest update, require a short self-audit:

- What the player learns.
- What the player proves in mechanics.
- What can go wrong and how the quest handles it.
- Why reward and prerequisites are proportionate.

### D) Encourage pair updates for docs + quest changes

Quest updates should include companion updates to the tree’s Skills docs in
`frontend/src/pages/docs/md/<tree>.md`, documenting gate logic and QA notes.

## Definition of done for v3 quest quality hardening

- [ ] The prioritized problematic quest list above has owners and tracking issues.
- [ ] Quest prompt docs include anti-pattern guidance and a self-review checklist.
- [ ] QA checklist §4.5 checkboxes are updated as manual validation progresses.
- [ ] At least one representative quest in each major new tree is manually validated before release.
