import Chip from './Chip.astro';

const { title, text, image, buyHref, sellHref, category, price } = Astro.props;

const safeBuyHref = buyHref ?? '#';
const safeSellHref = sellHref ?? '#';

<article class="item">
    <h3>{title}</h3>
    <div class="section horizontal">
        <img src={image} alt={title} loading="lazy" />
        <div class="section vertical">
            {category && <p class="metadata">Category: {category}</p>}
            {price && <p class="metadata">Price: {price}</p>}
            <p class="nobackground">{text}</p>
            <div class="section horizontal actions">
                <Chip href={safeBuyHref} text="Buy" />
                <Chip href={safeSellHref} text="Sell" />
            </div>
        </div>
    </div>
</article>

<style>
    .section {
        align-items: center;
    }

    .horizontal {
        display: flex;
        flex-direction: row;
    }

    .vertical {
        display: flex;
        flex-direction: column;
    }

    .item {
        background-color: #2f5b2f;
        padding: 20px;
        border-radius: 20px;
        opacity: 0.8;
    }

    .item:focus-within {
        outline: 2px solid #ffffff;
        outline-offset: 4px;
    }
    
    .item h3 {
        margin: 0px;
        text-align: center;
    }
    
    img {
        width: 100px;
        height: 100px;
        border-radius: 20px;
        margin-right: 5px;
    }
    
    p {
        background-color: #84d484;
        opacity: 0.8;
        color: black;
        padding: 5px;
        margin: 5px;
        border-radius: 20px;
    }
    
    .nobackground {
        background: none;
        color: white;
    }

    .metadata {
        background: none;
        color: #9be89b;
        font-size: 0.9em;
        margin-bottom: 0;
    }

    .actions {
        flex-wrap: wrap;
        justify-content: flex-start;
    }
</style>
