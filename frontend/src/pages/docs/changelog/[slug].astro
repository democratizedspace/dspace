---
const { slug } = Astro.params;
const anchor = slug ? String(slug).toLowerCase() : '';
const destination = anchor ? `/changelog#${anchor}` : '/changelog';

return Astro.redirect(destination, 301);
---
